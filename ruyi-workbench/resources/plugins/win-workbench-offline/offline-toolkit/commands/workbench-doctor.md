# Workbench Doctor

Run the local workbench doctor command and summarize the result:

```powershell
Ruyi.exe doctor
```

If the executable is not on PATH, locate it in the extracted offline package first.
