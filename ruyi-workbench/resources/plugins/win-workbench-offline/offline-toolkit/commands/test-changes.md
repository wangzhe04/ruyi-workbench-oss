---
name: 验证当前改动
description: 根据本地改动选择并运行风险相称的检查
---

# 验证当前改动

请检查当前未提交改动并做风险相称的验证：

1. 查看状态与差异，按功能、测试、文档、配置和生成文件分组。
2. 从项目脚本和 CI 配置找出最贴近改动的测试、lint、类型检查或构建命令。
3. 先跑最窄检查；通过后再决定是否需要更宽回归。
4. 不修改产品代码；如果验证本身需要改动，先说明并等待确认。
5. 汇总每条命令、退出状态、关键结果和未覆盖风险。

不要把“命令能启动”写成“功能已验证”。
