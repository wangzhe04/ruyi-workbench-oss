---
name: 安全检查
description: 针对当前改动检查真实可利用的安全风险
---

# 安全检查

请对当前改动做聚焦安全审查：

1. 查看差异并识别信任边界、外部输入、文件路径、命令执行、网络请求和密钥处理。
2. 运行本地安全扫描信号，但必须阅读命中代码后才能形成结论。
3. 优先检查注入、路径穿越、SSRF、XSS、越权、敏感信息泄露和不安全默认值。
4. 每个发现给出触发条件、影响、文件与行号、最小修复及验证办法。
5. 将“已确认缺陷”和“加固建议”分开；没有实质问题时明确说明残余风险。

默认只审查，不修改文件。
