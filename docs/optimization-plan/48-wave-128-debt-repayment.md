# 48 · 第 128 波「偿债波」—— 3.0 前把地基裂缝补上

> 状态：**开工**（2026-09-19）。上游：2.8.0 发布候选已推送（`83e9f7a`，标签与发布是用户动作，未做）。
> 3.0 发布门（2026-09-18 用户拍板，46 号文 §3、路线图 Pretender 行）＝ **① 本波** ＋ ② 128+ 全部既定项
> （H01、H02→H03、X01–X03、E02、114d／114e、22 号文引擎候选池；证据触发项「完成」＝走完证据门有结论）
> ＋ ③ 路线图 :86 的 P4 收尾硬门。本文只管 ①。
> 2026-09-19 用户：「我在别的电脑上进行人机测试，你直接按当前规划自主推进后续吧，一直推进即可」。
> **推送与打标签仍须用户明说**（上一次授权只覆盖那一批提交）。

## §0 为什么叫「偿债」

107 波连续撞到的几件事是同一层地基的裂缝，在它们上面叫 3.0，「更可控」这个承诺对不上：

- **默认值被冻在盘上**：`normalizeConfig` 是 `{ ...defaultConfig(), ...raw }`，而任一次写盘都把**整份合并配置**
  落下去 ⇒ 装过一次的机器上，每个键都实打实写着当年的默认值，此后产品改任何默认值，存量安装一个也吃不到。
  T1 只能靠一次性迁移补三个键；P1 又测出降级再升级会把用户关掉的开关重新打开（Brief §4.2 第 24 条）。
- **陈旧快照盖回新数据**：撤回被垂死回合的旧副本盖回（F7b 修了撤回这一条），但守卫是**按事故一条条手攒的**
  （路由覆盖、权限档覆盖、桌面工具覆盖、任务序号高水位、撤回代数，已第五个），其余读改写调用方跨在撤回上时
  「说 ok 其实没落上」（Brief 第 15–18 条）。
- **测试框架会误杀、会静默挂死**：`taskkill /T` 按父进程号找子孙，进程号复用时带走别人的树（F8 真杀过用户的
  Ollama）；各测试复制的 CDP 客户端在 socket 已关时静默挂到超时（Brief 第 19–20 条）。这两件让「全量绿」
  这句话本身不够可信。
- **G3 三个测试缺口**：简易模式零渲染覆盖、管家壳没有像素基线、人工键盘与真读屏没做。

## §1 切片

| 片 | 内容 | 出门判据 |
|---|---|---|
| **128a** | **配置只落「改过的键」**：显式键集合 `configExplicitKeysV1` ＋ 稀疏落盘 ＋ 迁移按显式键判（治冻结默认值，顺带治第 24 条） | §2 的七条行为各有单测；降级往返演练（真 2.8.0 包 ↔ 新版）；全量 |
| **128b** | **会话存盘版本闸通用化**：把五个手攒守卫收成一处「会话代数」机制；读改写调用方跨撤回时不再「说 ok 没落上」（15）；旁车笔记过撤回闸（16）；撤回停驱动器续跑（17）；`rewind_superseded` i18n（18） | 负载复现修前红、修后绿；每个旧守卫的原有断言照绿 |
| **128c** | **测试框架进程与 CDP**：一个共享的「只杀自己的树」助手（核创建时间）替换各件复制的 `taskkill /T`；CDP 客户端 socket 关闭即拒绝在途请求 | 机械锁：仓里不再有裸 `taskkill /T`；CDP 关闭即报错的单测；全量 |
| **128d** | **G3 缺口的可自动化部分**：简易模式真浏览器渲染件；管家壳两主题像素基线；全键盘走查件（Tab 顺序、焦点可见、Esc 回退） | 各件反向验证；真读屏走查留给人（用户在另一台机器做人机测试） |
| **128e** | **安全**：MCP `resources/read` 原样返回 `config.json`（明文 API Key，模型可经 MCP 资源读到——**先核实可达性**）；`baseUrl`／`audioBaseUrl` 私网与回环准入（第 8 条） | 核实结论 ＋ 修法 ＋ 锁 |
| **128f** | **小债批**：模型菜单首开挪位（21）；切回管家闪「读取中」（23）；账本即发即忘（5）；管家拒代批干等与 `2]&1`（7）；索引重建竞态（22）；`mission-index-scale` (e) 冷详情 P95 门余量只有约 3%（基线实测 474–777 ms／门 800，128c 全量里第二次出现）—— 查冷路径慢在哪；`classic-window-live-steer` H8 竞态（128a 记录） | 逐条各自判据 |
| **128g** | **覆盖包**（第 25 条）：跨版本修好，还是把「覆盖包升级」这条路收掉只留同版本补丁 | **产品选择，里程碑时列给用户**；不阻塞其余切片 |
| **128h** | **记忆三门**（J03／J12／J13 缺的半边，Brief §4.2 A 表与 B1–B3） | 各门判据补齐并有读数 |
| **128i** | **产品侧收尸也只认自己的子孙**（128c 顺带查出）：产品里三处 `taskkill /T` —— `04-permission-runtime.js` `killChildTree`、`13-http-router.js` `freeStalePort→killPid`（杀占端口的旧工作台）、`04-desktop-shell.js` 超时整树杀 —— 与测试框架同一撞号隐患，但发生在用户机器上。把 128c 的「按创建时间认子孙」搬进 `src/` | 单测（合成进程表）＋ 真进程（detached 孙子被收、撞号陌生人不碰）；三处调用点换掉并配锁 |

执行序：**128a → 128c → 128i → 128b → 128e → 128d → 128f → 128h**；128g 等拍板。
128c 提前：后面每一片都要靠全量说话，先把「全量绿」这句话修可信。

## §2 128a 设计（主会话定）

**语义**：没碰过的设置跟随产品当前默认；碰过的设置永远照你的。

1. **显式键集合** `configExplicitKeysV1: string[]`，与 `configSchema` 一样是簿记键，随盘落下。
2. **落盘投影**（唯一写口 `writeConfigAtomic` 的输入）：`configSchema`、`configExplicitKeysV1`、**显式键**的值、
   以及**本版不认识的键**（原样保留——更新的版本写下的键，降级时不能被我们抹掉；2.7.0 正是这样保住了 2.8.0 的键）。
   等于默认值且不在显式集合里的键**不落盘**。
3. **什么算显式**：
   - 经 `mutateConfig` 的一次写入里，归一化后**值变了**的键（设置页、API、管家改设置、产品代用户记的状态如
     `lastUsedEngineRoute`，一律同一口径——「被改过」）；
   - 读盘时，盘上**出现**、且归一化后**不等于当前默认**的已知键（手改 `config.json`——手册就教用户这么关开关；
     以及旧版本写下的整份配置）。
4. **迁移按显式键判，不只按 schema 号**：`< 12` 那道三开关迁移跳过显式集合里的键 ⇒ 用户在新版里关掉的，
   降级到 2.7.0（它把 schema 写回 11、但保留不认识的 `configExplicitKeysV1`）再升回来也不会被重开（治第 24 条）。
   `< 9` 的 `engineMode` 迁移同样跳过显式键。
5. **首读即清理**：盘上等于默认、又不在显式集合里的键在第一次读时就被写回投影清掉 —— 不清的话，下次默认
   变了它们会被第 3 条的「手改」规则误认成显式，又冻住。
6. **`changed` 改判据**：是否写盘 ＝ 投影与盘上内容是否不同（不再是归一化过程中的内部标记）⇒ 派生值
   （如没设工作区时 `defaultWorkspace` 取家目录）每次读都重算、但不在投影里，不会造成每读必写。
7. **旧文件**（schema < 13，含降级往返后的）：第 3 条第二款把与当前默认不同的键都记成显式 ——
   等价于今天的行为（不会比现在更差），从此以后才享受「没碰过的跟默认走」。已知代价：降级到旧版期间旧版写下的
   **与新版默认不同的旧默认值**会被当成显式冻住（与今天相同）；在旧版里做的改动**保留**（不丢用户数据优先）。

**粒度**：键级。嵌套对象（`desktopMcp`、`storagePolicy`…）里某个子字段的默认变化，仍要走迁移。

**不改**：`normalizeConfig` 返回的内存视图照旧是整份（所有消费方零改动）；`GET /api/config` 形状不变。

## §2-b 128b 设计（主会话定，2026-09-19；普查由只读代理出表、主会话逐条复核后落刀）

**普查结论**（54 处真调用）：一次被撤回闸丢掉的 `saveSession` **返回值与成功时一模一样**（`02:3143` 返回 `session`），
只有传了 `throwIfStale` 的调用方（今天只有 `updateSessionMetaAttempt` 与撤回自己）知道自己没落上。分类：
- **回合自存**（约 30 处，`runClaudeTurn`／`runOpenAiTurn`／Kimi／`maybeAutoCompact`／steer 排空）：垂死回合的写被丢是**对的**，不动。
- **请求驱动的读改写（会回「成功」却没落上）**：任务开始／更新／核验（`13:1002`、`13:1034`）与任务控制（`02:2325`）——中间还要跑核验命令、
  停子代理，窗口最宽之一；**手动压缩三个引擎**（`10:2204`、`10:1877`、`05b:289`）——中间是数秒的摘要模型调用，回 `ok:true` 带 token 读数；
  技能（`13:153`）、记忆（`13:686/705`）、todo（`13:965`）；管家拜访归档（`13q:452`）。
- **后台读改写**：工作流摘要追加（`08:2080`）、管家回复盖章（`13p:614`）。
- **旁车**：会话笔记（`writeSessionNotes`）、任务变更流水（`bumpMissionChangeSeq` 在被丢的保存之后照样记一笔）、历史快照 —— 都不看撤回代数。
- **驱动器**：任务驱动器在回合之间拿着聊天流那份对象继续跑；撤回只停 `activeChildren` 里的回合 ⇒ 撤回落在两回合之间时，驱动器接着起回合、
  每一存都被静默丢掉（Brief 第 17 条；普查代理提出，**待主会话实证**）。

**设计**：
1. **`mutateSession(id, mutator, { writer, expectGen })`**（02，与 117n-M2 的 `mutateConfig` 同一个形状）：`loadSession` → `mutator(session)`
   （可返回 `{ abort }`）→ `saveSession(session, { throwIfStale: true, writer })`；撞上 `SESSION_STALE_SAVE` 就重新读、重新应用，最多 3 次；
   仍不行抛 `session.rewound_during_write`（调用方回可读的错误，不回成功）。
2. **慢活不重做**：压缩与任务核验这种「先花几秒算、再落盘」的，算之前记下 `rewindGen`，落盘走 `mutateSession(…, { expectGen })` ——
   新读到的代数变了（期间撤回过）就 `abort`，回 `session.rewound_during_write`，**不把基于撤回前历史算出来的摘要写回去**。
3. 上面点名的请求驱动与后台读改写全部改走 `mutateSession`；**变更流水只在保存真落上之后记**。
4. **旁车过闸**：`writeSessionNotes` 与历史快照写之前比一次代数，陈旧就跳过并记一条日志。
5. **驱动器**：每起一个回合之前判「这份对象是不是撤回之前的」（`sessionObjectIsStale`），是就停并记原因。
6. `rewind_superseded` 进 i18n（四份 locale）。
7. **锁**：请求驱动的路由里凡调 `saveSession` 的，要么走 `mutateSession`、要么显式 `throwIfStale`（静态扫描 ＋ owner 数钉住）；
   `mutateSession` 的单测覆盖「撤回插在读与存之间 ⇒ 重做一次后落上」「expectGen 变了 ⇒ 不写、报错」。
**不做**：三个「用户刚切的值在存盘时重新盖一次」的覆盖表（路由／权限档／桌面工具）今天是好的，合并成一张表只是换写法、有风险无收益 —— 不动。

## §2-c 128f-③ 设计：`/api/status` 不再等桌面组件的 Python 探测（主会话定，2026-09-19 下午）

**读数**（HF2 冒烟与追踪，合成数据、隔离家目录）：Full 包首个 `/api/status` **8.7–8.9 s**（冷）／3.0 s（温），第二个 0.18 s。
`--require` 预载追踪（同步三件＋`ChildProcess.prototype.spawn`＋fetch／http(s)／dns＋事件循环卡顿）定位：请求期间事件循环几乎不卡，
慢在**等** —— 状态路由在 `computeHealth`、`generateMcpConfig`、`desktopMcp` 三处 `await ensureDesktopMcpWarm()`，而那趟预热就是
Full 包内嵌 Python 跑 `from mcp.server.fastmcp import FastMCP`（温 2.6 s，冷更久）。39 号文当初「等」是对的（不等就走同步探针，钉住整个进程），
但代价是**整个界面**等 Python。进程内缓存肯定结果只活 5 分钟 ⇒ Full 包**不只首启**：隔 5 分钟以上的每一次启动都要付。

**判据**：
1. 探测在飞（缓存冷）时，`/api/status` **不等、也不走同步探针**：`desktopMcp` 回 `{ enabled, detected:null, resolved:null, probing:true }`；
   健康项 `desktop-control` 回既有的 `preparing` 态（前端已有人话，不新造状态）；`mcpConfigPath` 回路径本身、不重新生成（启动链在预热完后本来就生成一次）。
   探测已完成（缓存热）时，输出与改前**逐字段相同**。
2. 「缓存冷不冷」不另写一套判据：同一个 `detectDesktopMcp()` 在**只读缓存**模式下跑一遍 —— `pickPython` 在该模式下遇到缓存未命中
   只记一笔、不探；记到了就是「在飞」，并顺手踢一脚预热（幂等，与启动那一趟共用）。
3. 前端：看到 `probing` 就起一个**有界**的跟进（每 1.5 s 一发 `/api/status`，至多 60 s），拿到非 probing 的那一份后**只**替换
   `desktopMcp`／`health`／`mcpConfigPath` 三个字段、只重画桌面组件那一行状态与体检面板 —— **不调 `fillSettings`**（不然用户正在设置页里
   改到一半的东西会被整页回填冲掉，「写回没读到的状态」那个模具的镜像）。
4. 测试口：`WCW_TEST_DESKTOP_PROBE_DELAY_MS` 只在异步探针里加一段延时，让「在飞」窗口确定性地存在；件里另把子进程的 `TMP/TEMP`
   指到新目录，绕开跨进程磁盘缓存。

**不在本刀**：`detectClaudePath`／`detectKimiPath` 的同步 `--version` 探针（启动时共 ~1.7 s 卡在 listen 之前）；Python 探针本身改成
`find_spec` 那种更轻的判法（改判据语义，另议）。

## §3 纪律（每片都适用）

- 进程安全：只杀自己 spawn 的 PID（认子孙须核创建时间）；禁按名字杀；开工与收工各拍一次进程快照对照
  （[46 号文 F8 事故](46-wave-107-release-approval.md)）。用户的正式工作台（`dist/Ruyi-slim`，8765）与 Ollama 是禁区。
- 回归：`run-all --parallel 4`，控制台存档到文件、读 `EXIT=`；**跨轮重现的偶发件按真缺陷取证**。
- 生成器链在最后一次 `src/` 改动后整条重跑；控制字符／CR 扫描；反向验证每把新锁。
- 起服务的演练只用合成数据。
- 本地提交，不推送。

## §4 交付记录

### 128a · 配置只落「改过的键」（2026-09-19，主会话亲做）

**实现**（`01-config.js`、`00-boot.js`、`14-main.js`）：簿记键 `configExplicitKeysV1`；`normalizeConfig` 返回 `{ config, changed, persisted }`，
`changed` 改判「投影与 raw 是否不同」；`writeConfig(next, before)` 按「这次写入里值变了」加显式键，`mutateConfig` 在 mutator 动手前深拷
`before`；mutator 删掉已知键 = 恢复默认；`version`／`configSchema` 是簿记键（旧文件的 `'2.7.0'` 不被钉住）；`claudePath` 按用户给的原值判与落盘
（Symbol 键 `CONFIG_GIVEN_CLAUDE_PATH` 携带，JSON 不序列化），真身 exe 路径不进盘；`< 9`／`< 12` 两道迁移跳过显式键；`CONFIG_SCHEMA` 12 → 13。

**第一轮全量红 15 件 —— 其中是我自己的一个丢数据缺陷**：推断显式键时只看 raw 里出现过的键，而 `incomingConfigSchema < N` 的迁移会给 raw 里**没有**
的键造出值（`< 10` 从 `defaultWorkspace` 给工作区表播种）。第一次读不落盘、schema 一抬到 13 播种再也不跑 ⇒ **工作区表被清空**，管家开线程一律
`invalid_request`（11 件连坐）。全新安装同样中招 —— 我自己写的单测 [A] 断言「全新安装只落簿记键」，**恰好把这个缺陷钉成了对的**。
修法：旧格式那一次读（schema < 当前，含全新安装与降级往返）看**全部**键，不等于默认的都记显式；当前格式只看 raw 里的键（手改）。
[A] 改正、新增 [M]（迁移造出的值在那一次读就落盘并活过下一次读）。

**另 6 件是测试把「盘上有每一个键」当前提**（改的是判据的读法，不是放宽）：`uimode-style`（`uiMode:'simple'` 就是默认、这一发没改它 ⇒ 不钉住；改为判
`outputStyle` 落盘且进显式集合）、`config-read-safety` F1（全新安装是稀疏文件）、`websearch`（折叠成 builtin 后等于默认不落盘，真正该落的是
`searchBackendMigrated` 标记位）、`steward-guardrails` E2（缺键 = 出厂默认）、`steward-deliverable`／`steward-quick-ask`（把盘上的稀疏文件直接当整份配置传给
`startStewardInbox`，`stewardEnabledV1` 读成 undefined ⇒ 当成管家关着；改为先 `normalizeConfig`）。另有两把静态锁的 `CONFIG_SCHEMA = 12` 与
`steward-config.static` 的幂等断言按新语义改（幂等钉在盘上那份投影上），`steward-config-tier` 给新键分级 `forbidden`（管家若能改它，就能把任意键
「去显式化」绕开用户设过的值）。**产品代码里没有第二个直读 `config.json` 的地方**（全仓搜过）。

**锁**：`dev-harness/unit/config-explicit-keys.test.js`（14 组，真读盘真写盘：[A] 全新安装、[B] 旧整份文件、[C] 改过即钉住、[D] 手改、[E] 不认识的键、
[F] 迁移按显式键判、[G] 降级往返、[H] 不每读必写、[I] version 戳、[J] 删键 = 恢复默认、[K] claudePath 原值、[L] 投影幂等、[M] 迁移造出的值落盘）。

**真包往返演练**（合成数据、隔离家目录；2.8.0 Slim 包 ↔ 仓里新代码）：2.8.0 全新写出 155 键（schema 12）→ 新版读：盘上 **6 键**、与 2.8.0 的有效配置
**差异 0**；经真 `POST /api/config` 关掉 dedup → 7 键、进显式集合；降级到 2.8.0（整份回写 156 键，`configExplicitKeysV1` 作为不认识的键保住）→ 2.8.0 照样读到关；
回到新版 → **仍关**，与往返前的有效配置差异 0。收工无残留进程。（演练脚本第一版在 PowerShell 5.1 `Start-Process` 里没给带空格的仓路径加引号，node 执行到
`Documents\Claude` 这个无扩展名文件——一个 JS 包——立即因缺原生桥报错退出，无副作用；已加引号重跑。）

**全量**：第二轮 **355/1**（`EXIT=1`）。那一件是 `classic-window-live-steer` H8（两次都红）：本页自己的回合收尾、H8d 已确认服务端释放这条会话之后，
点下发送时服务端**真的受理了插话**（审计多一条 `intervention/source:steer`）⇒ 这两步之间有别的回合在这条会话上起来了（管家已被 H8pre 关掉）。
配置路径逐项核过（H8pre 那一发 POST 的稀疏体、落盘、回读都对；有效配置与基线逐键相同），机制未定。**读数**：128a 树单跑 2/≈40 红（一次在头三跑、一次在
下五跑；之后 8 次单跑、12 次四路并行、8 次加压全绿，加压那一轮另一路的 `quiet-card.browser` 超时是我人为让独占件并行造成的，不算数）；基线 `83e9f7a`
（临时 worktree）**0/17**。按失败率约 7% 算，基线 17 跑全绿的偶然概率约 29% ⇒ **分不清是不是 128a 带来的**。**不当噪声放过**：登记为未结项，
下一轮全量再红就在那一跑里取证（探针形状已验证可用：保留夹具目录、在 H8h 后打出会话尾与审计尾）。

### 128c · 测试框架只杀自己的树 ＋ CDP 关了就当场报错（2026-09-19，主会话亲做）

**取证**（scratchpad 探针，只动自己 spawn 的进程）：想直接证明「taskkill /T 会不会带走父号过期、恰好撞号的陌生进程」，没做成 ——
第一版探针同步循环里事件循环不跑、Node 手上的进程句柄不关，**6000 次 spawn 一次都没复用到旧号**（反过来坐实了「句柄在手 ⇒ 号不会被复用」）；
改成每个都等 exit 之后，**3 万次 spawn 也没撞上**。所以 /T 是否核对创建时间**仍未验证**，不赌它。顺带撞见两件真事：
① **libuv 把非 detached 的子进程放进「句柄关闭即杀」的 job** ⇒ 根一死，它们跟着没了；需要按树收的是逃出这个 job 的（detached、非 node 父进程拉起的）；
② **先杀 `conhost.exe` 会把同一控制台上的孙子连带杀掉**，核验脚本自己也没了输出（退出码 1）⇒ 不杀 conhost，它会随最后一个客户端自己走。

**实现**：`dev-harness/lib/kill-own-tree.js` —— 根只杀【还没退出】的自己的子进程（句柄在手，号一定还是它；已退出的不碰）；子孙从一次进程表快照里按父号往下找，
**只认创建时间不早于父进程的**（撞号的陌生人一定比我们的根更老，按构造排除）；动手前按 FILETIME 再核一次启动时间；逐个 `/PID` 不带 `/T`；conhost 不杀、深的先杀。
纯函数 `ownDescendants` 单独导出。codemod：**250 个测试文件 268 处** `taskkill /PID … /T /F` → `killOwnTree(x)`，外加 run-all 的超时收尸与 `kimi-acp-live-probe` 手改；
**27 个自带 CDP 客户端**的 `send()` 在 `socket.send` 前加 readyState 闸（socket 已关时 `WebSocket.send()` 按规范静默丢弃 ⇒ 修前那个 Promise 永不 settle，测试挂到超时、一条 FAIL 都没有）。

**锁**：`unit/kill-own-tree.test.js`（[P1] 合成进程表里撞号的陌生人及其子孙都不算、真子孙含孙辈一个不漏；[P2] 快照解析；[R1] detached 孙子由树遍历收掉；[R1b] 普通孙子随根走；[R2] 根已退出不碰）；
`process-safety.static.e2e.js`（① 全框架零 taskkill /T 调用、run-all 超时走 killOwnTree；② 每个 CDP 客户端都有闸；③ owner 数钉成 251／27）。
**反向**（逐次备份还原、sha256 一致）：塞回一处 `taskkill /T`（数组形与模板串形各一）→ ① 红；拿掉一个 CDP 闸 → ② 红点名文件行号；删掉 lib 的创建时间比较 → 静态锁与 [P1] 同时红。
**反向验证逮住了锁自己的洞**：① 第一版正则写 `[^)]*`，真实形状里的 `String(child.pid)` 让匹配提前停住 —— 锁对原形是瞎的，已改。行为抽查：把闸摘出来单跑，关着的 socket 上 `send()` **0 ms 拒绝**、pending 不留。

**全量**（`d1c7b7d`＋本片工作树）：**356/1、1 flaky**，退出码 1，26.5 min（比之前约多 3.5 min —— 每次收尸多一次进程表快照）；**进程快照前后一致**（Chrome 主进程 15692、webview2 6、node 1、ollama 0、explorer 1248）。
- 红的那件 `steward-tools` 是**本片的时序副作用**、测试自己的缺陷被放大：它等「输出里出现 `"tools"`」，而 initialize 的回包 capabilities 里就有 `"tools"` ⇒ K3 在 tools/list 发出去之前就算过了（**K3 修前是空判**）、
  子进程被杀，800 ms 那一发再往死进程的 stdin 写；killOwnTree 同步取进程表占住事件循环，这一写落在退出事件被处理之前，变成没人接的异步 EPIPE，整件崩。改为等 tools/list 自己的回包（id 2）、收尾清定时器、stdin 错误有人接；单跑 3/3。全框架同形（定时器里往子进程 stdin 写）只此一处。
- flaky `mission-index-scale` (e)「详情冷 P95 ≤ 800 ms」实得 843 ms（**第二次出现**，按规矩取证）：基线 `83e9f7a` 单跑三次 777／495／474 ms、当前树 549／575／534 ms，其余三项读数两边相同；
  `normalizeConfig` 微基准当前树反而更快（79 µs 对 126 µs —— 盘上从 5167 字节变成 756 字节）⇒ 不是 128a 拖慢的；**真问题是这道门余量只有约 3%**（基线自己就跑出过 777）。记进 128f：查冷详情路径慢在哪。

### 128i · 产品侧收尸也只认自己的子孙（2026-09-19，主会话亲做）

**实现**（`04-permission-runtime.js`）：唯一实现是一段 PowerShell（`OWN_TREE_KILL_PS`）：进程表快照 → 从根按父号往下找、**只认创建时间不早于父进程的** →
根最先、深的先杀、conhost 不杀 → 动手前逐个按启动时间（FILETIME）再核一次。`-EncodedCommand` 传（不经任何引号转义）。两个入口：
`killChildTree(pid)`（14 处调用点零改动，发出去就算）与 `killOwnProcessTree(pid)`（等它跑完；`13-http-router.js` `freeStalePort` 的 `killPid` 改走它）。
dry 模式喂合成进程表、只打计划，供单测钉「撞号的陌生人不算子孙」。

**「发出去就算」怎么发，是取证定的**（scratchpad 探针，只写标记文件）：
- Node 的 `detached`（`DETACHED_PROCESS`，没有控制台）起 `powershell.exe`：**退出码 0、脚本一行都没跑**（3/3）；
- 直接 spawn：能跑，但在本进程的 libuv job 里 —— 服务收尸完马上退出时 job 一关它也被杀（修前 taskkill 约 80 ms 多半抢得赢，这段约 0.5 s）；
- 经 `cmd /d /s /c "start "" /b powershell …"`：PowerShell 成了 cmd 的孩子，libuv 的 job 允许孙辈静默脱离 ⇒ **父进程 150 ms 就退出、脚本 1.5 s 后照样跑完**（2/2）。
  `-EncodedCommand` 的参数只含 `[A-Za-z0-9+/=]`，cmd 里没有注入面；命令行超 7800 字符（不会）退回直接 spawn。

**单测的一处假绿**：PowerShell 5.1 里 `@(ConvertFrom-Json '[…]')` 得到的是「一个元素、里面装着整个数组」—— 第一跑 [P1] 报 `ROOT-GONE`，
**[P2]「根不在表里」因此是为错的理由绿的**；显式展开后两条都按判据绿。

**锁**：`unit/kill-own-process-tree.test.js`（[P1] dry：撞号陌生人及其孩子、conhost 都不在计划里，根最先、深的先；[P2] dry：根不在 ⇒ ROOT-GONE；
[R1] `killOwnProcessTree` 真收掉根与 detached 孙子；[R2] `killChildTree` 几秒内根与 detached 孙子都没了）；`process-safety.static` 加 ④（`src/` 53 个模块零
`taskkill /T` 调用、PowerShell 里的创建时间比较与 conhost／启动时间两行、两个入口的接线）。**反向**：删掉创建时间那一行 → 静态锁 ④ 与 [P1] 同时红；还原 sha256 一致。

**全量**：**356/1、1 flaky**，退出码 1，进程快照前后一致（explorer 1248 等）。
- 红的是 `classic-window-live-steer` H8f／H8h（两次都红）—— **128a 登记的未结项第二次在全量里红**（上一次 128a 第二轮）。这一次的形状与 128a 单跑那次相同：
  本页回合收尾、服务端已释放之后按下发送，服务端受理成了插话。**下一步**：把诊断放进测试本身（失败时打会话尾／审计尾带时间戳、保留夹具目录），下一轮全量就在那一跑里取证。
- flaky `walkthrough-round1` C2（「管家视角：点行里那块空白 → 焦点真的换了」首跑红）—— **126 波那次 C2 之后第二次出现**，按规矩取证（进 128f）。

### 128b · 会话「读-改-写」唯一原语 ＋ 旁车／驱动器过撤回闸 ＋ 撤回相关的码有人话（2026-09-19，主会话亲做；普查由只读代理出表、逐处复核后落刀）

**原语**：`02` 的 `mutateSession(id, mutator, { writer, expectGen })` —— 新读 → mutator（可 `abort`／带 `value`）→ `saveSession(…, { throwIfStale: true })`；
撞上撤回就重新读、重新应用，最多重放 2 次，仍不行抛 `session.rewound_during_write`；`expectGen`（慢活：算之前记下的撤回代数）与新读到的不一致、
或读时一致但撤回落在存之前，一律抛、不重放。另出 `sessionObjectIsStale(session)`。

**改走它的十一处**（都是普查表里「会回成功却没落上」的）：技能（`setSessionSkillsCore`，路由回 409 稳定码、管家工具回 `version_conflict`）、线程记忆两支、todo、
任务账本 check（慢活：先真跑核验、结果按里程碑 id 收起，落盘带 `expectGen` 重放到新副本）／start／update（mutator 可重放，`mission_start` 日志挪到落盘成功之后）、
任务控制（停回合／停子代理／撤授权／回退这些副作用不可重做 —— 只把「账本该是什么样」整份重放到新副本；撤回不动账本）、三个引擎的手动压缩
（provider 与外部摘要带 `expectGen` ＋「期间历史长度变了就不写」—— 否则这份摘要会把期间追加的几条整段盖掉；Kimi 原生压缩发生在 Kimi 那边、不管撤没撤回都已经发生，
本地记录照重放，但新副本若已不指向同一个原生会话就不写）、工作流摘要追加、管家回复盖章、管家拜访归档（归档文件先写、截断带 `expectGen`，没截成就把刚写的归档删掉 ——
修前是「归档了、会话里也还在」同一段对话存两份）。**变更流水只在保存真落上之后记**（任务路由与任务控制都是）。

**旁车与驱动器**（Brief 第 16、17 条）：会话笔记（`maybeWriteSessionNotes`）与历史快照（`maybeAutoCompact` 里）遇到陈旧对象不写；任务驱动器每起一回合前判陈旧，陈旧就收手记原因。
**第 17 条原是普查代理提出、标「待证」—— 反向验证坐实**：拿掉那一行，驱动器拿着撤回之前的对象照起了 1 个回合（修后 0 个）。
**i18n**（第 18 条）：`rewind_superseded` 进 `LEGACY_API_ERROR_CODES` 归一成 `session.rewind_superseded`；工作台 `API_ERROR_I18N` 与管家抽屉 `failNote` 都给三个码配了人话键，四份 locale 各三句。
修前管家壳把原串 `rewind_superseded` 摆给用户。

**不做**：三个「用户刚切的值在存盘时重新盖一次」的覆盖表不合并（有风险无收益）；**活回合那一侧的丢失更新**（回合收尾存拿着旧对象盖掉并发的读改写）是另一类问题，
各有既有守卫（覆盖表、任务账本收尾合并、元数据补丁的延后通道），本片不扩大范围。

**锁**：`unit/session-mutate.test.js`（[M1] 基本；[M2] 撤回插在读与存之间 ⇒ 第一次写被闸丢掉、重放一次落上，撤回的截断与这次改动都在；[M3]／[M4] expectGen 两种形状都抛、不写、不重放；
[M5] abort；[D1] 陈旧对象驱动器零回合；[D2] 对照照常起回合）；`session-save-census.static.e2e.js`（① 各模块 `saveSession(` 普查数与表逐项相等 —— 新加一处就红、红的那句话让人回来判「回合自存」还是「读改写」；
② 点名十一处真的走 mutateSession（慢活带 expectGen）、没有裸存；③ 原语带 throwIfStale／expectGen；④ 旁车与驱动器的闸；⑤ 稳定码、两壳人话键、四份 locale）。
**反向**：驱动器那一行删掉 → [D1] 红（照起 1 回合）；mutateSession 存盘去掉 throwIfStale → [M2] 红（只写 1 次、改动丢了）；技能核心塞回一处裸 `saveSession` → 普查 ① 与点名 ② 同时红。均按备份还原、sha256 一致。
**顺带逮到两处**：`compact-marker-merge` 单测在 vm 沙箱里跑 `maybeAutoCompact`，缺了新引用的 `sessionObjectIsStale` 会被吞成「没压缩」（它头注记着同一个坑）—— 注真函数；
普查锁第一版把路由路径写成字面串，`route-inventory` 把它当成 6 条路由的覆盖（它只读源码、不打路由）—— 改成按片拼、清单回到 8 条未覆盖。

**全量**：**356/2、1 flaky**，退出码 1，进程快照一致。两红是 `mission-result` s13 与 `acceptance-provenance` f2 —— 两把静态锁钉着任务核验路由的旧形状：
s13 放宽到接受 `fresh`；f2 从「落章紧跟着检查（400 字内）」**改钉这条数据链本身**（真跑 → results → byId → 取回 → 落章），反向：把取回改成凭空构造的结果 → f2 红。改后两件 2/2 绿。
flaky `steward-quick-ask` I3（「会话头 turnSeq === 1，got 0」）**首次出现**，登记跟踪。`classic-window-live-steer` H8 这一轮没红（诊断已在测试里待命）。

### 128e · MCP 资源面交出明文密钥（**实测坐实**，已修）＋ URL 准入按证据结案（2026-09-19，主会话亲做）

**先核实可达性**（计划里写的）：新件 `mcp-resource-config-mask.e2e.js` 起真 `server.js mcp` 子进程（隔离家目录、合成配置、三处埋假密钥：provider apiKey、Claude CLI
`modelsApiKey`、外部 MCP 连接器 env 里的令牌），走 `initialize → resources/list → resources/read`。**对修前代码跑：三处假密钥全部明文读出**（R2／R3 红）。
`resources/list` 列出一条「Workbench config」，`resources/read` 把 `config.json` 原样读出返回 —— 连到工作台 MCP 的模型（Claude 引擎会话；以及用户自己 Claude Code 里登记了
工作台 MCP 的会话 —— 本机当下就是）都能用读资源的工具拿走。同一批值在 `GET /api/status` 上 107-S0／S0b 早就掩了，文件工具也把 `config.json` 列为敏感路径拒读 —— 这是绕过两道门的第三条路。
**已推送的 2.8.0 发布候选（`83e9f7a`）里就有这个洞**（标签与发布是用户的决定，见里程碑汇报）。

**修法**（`13-http-router.js` 资源读取那一支）：交出去的是与 `/api/status` 同一份掩码视图 `maskProviders(await readConfig())`（不另写一套），
也顺带解决了 128a 之后「盘上是稀疏文件、原样读给不出当前生效配置」的问题。修后同一件：**0 泄漏**、provider 密钥是 `••••e5f6` 并带 `hasKey`、仍是合法 JSON（6599 字的整份视图）。
MCP 相关 e2e 子集 7/7 绿。`route-inventory` 给这条 MCP 判定点记上本件的覆盖 —— 这一次是真覆盖（它真的发 `resources/read`）。两把件数锁（`fixture-home` RUYI_HOME spawn 149→150、`process-safety` killOwnTree 251→252）按来路同步。

**URL 准入（Brief 第 8 条前半）按证据结案、不改**：拒绝私网／回环地址会直接打坏合法的本机端点（Ollama `127.0.0.1:11434`、LM Studio、本地 ASR）。风险面要看「谁能写这些地址」：
`steward-config-tier` 里 `providers`／`searchBackend` 是 **forbidden**（管家碰不到），`modelsApiBase`／`externalMcpServers` 是 **confirm**（必须用户亲手确认）⇒
所有带 URL 的配置都只能由用户设（直接或亲手批准），不存在不可信主体把服务指向内网的路径。**后半（ASR 每会话 ≤1／队列 3 的并发上限从未实现）挪进 128f。**

### 2.8.0 热修线（插在 128e 与 128d 之间，2026-09-19；用户选 A）

128e 查出的 MCP 资源面明文密钥**在已推送的发布候选 `83e9f7a` 里就有**（自 v1.4.0 起）。用户选 A：`release/2.8.0` 从 `83e9f7a` 切出、只移植这一处
（`53e1402`，修前三把假密钥全漏、修后 0 漏），全量 `357/0/0 flaky` 退出码 0，两个包重打（full `501c63ac0ddd…`／slim `162e23c92582…`）并在全新目录冒烟
（三种入口、MCP 资源面在 node 与 exe 两个入口下都 0 泄漏），记录提交 `459313b` —— **`v2.8.0` 标签打在它上面**。细节在 46 号文 HF 一节。
master 上同样的修法早在 `121e5c4`，两条线不合代码；master 的 CHANGELOG 2.8.0 节待补那条「第四处泄密面」（与 3.0 节一起补）。

### 128d · G3 缺口的可自动化那一半：简易档渲染、键盘走查、小审计器、管家壳像素基线（2026-09-19，主会话亲做）

**先立公共夹具** `dev-harness/lib/browser-fixture.js`：假 provider、隔离家目录的工作台、无头 Edge、CDP 客户端（看门狗＋readyState 闸＋事件订阅）、只杀自己的树。
**在它之前每件真浏览器测试各自复制一份 CDP 客户端（27 份）**，128c 修「socket 关了静默挂死」就得改 27 处；四件新件一律用它。run-all 按「require 了它」认出
开浏览器的件（原先只认源码里的 `--user-data-dir=` 字面量，写在 lib 里就认不出、不给 profile 范围）；新件里两件进独占桶（键盘、像素）。
锁跟上：process-safety owner 数 27 → 28／252 → 253；fixture-home 把这一个 lib 纳入 RUYI_HOME 扫描面（150 → 151）并钉「夹具 close() 收浏览器、用它的件都 await 过 close()」。
像素尺子抽成 `lib/png-grid.js`（dom-screenshot 改用它，阈值逐字不变、单跑照绿）；`unit/png-grid.test.js` 手搓五种行过滤器的 PNG 钉解码逐字节正确
（反向：Paeth 支改成 Up → 两条解码用例红，还原 sha256 一致）。

| 件 | 量什么 | 修前读数 | 修了什么 |
|---|---|---|---|
| `simple-mode.browser` | **出厂默认档**（配置不写 uiMode）：属性与 15 px 字号；两视角 × 1440／640 不溢出；右栏藏「用量／记录」；设置只剩白名单六枚、逐枚点得开、藏着的点不到；真回合（输入框 Enter 发送、假 provider 要一次 file_read）工具卡显示人话动词（对照：切专家档反过来）；齿轮切换落盘；全程零未捕获异常 | **2 红**：① `file_read` 的卡在动词槽里原样写着 `file_read` —— 动词表是给**授权弹窗**写的、只收会弹窗的改／执行类，**出厂默认档里最常见的读类工具全是英文标识**；② Esc 一下关不掉设置弹窗 | ① 补齐全部原生工具（36 条动词 ＋ `workbench_memory_`／`steward_` 两条前缀，中英两份 ＋ `docs/i18n` 真源同步）＋ 新锁 `tool-verb-coverage.static`（产物 `TOOL_HANDLERS` 97 个逐个有人话、每个键两份 locale 都有；反向：换回旧表 → 75 个没有）；② 见下一行齿轮菜单 |
| `keyboard-walkthrough.browser` | 两视角 × 深浅两主题从跳转链接起 Tab 一圈：每站焦点看得见（轮廓／阴影／祖先 :focus-within）、在视口里、能绕回（无陷阱）；Shift+Tab 镜像；齿轮菜单 ↓↑ Home End Esc；设置弹窗焦点陷阱与关后焦点去处；Ctrl+` 与背靠背快切；焦点卡 Enter／Esc；管家输入框 Tab 契约 | **首跑 13 红**，产品问题 5 处：**齿轮菜单自称 role="menu" 却只认鼠标**（打开后焦点留在齿轮钮、方向键无反应）；**按下「设置」菜单不收** —— 它那条 Esc（document 上、stopPropagation）先收看不见的菜单，弹窗要按第二下（简易档 S4f 同一根）；**当前页签拿焦点时与没焦点一模一样**（`.tool-tabs button.active` 的投影压过全局焦点环，深色实测零差别）；**弹窗关后焦点掉到 body**（触发它的菜单项随菜单藏起来了）；**快切视角两处**（被跳过的过渡 `ready` 没人接 → 页面冒 `AbortError`；「切到另一边」读还没落地的旧属性 → 背靠背两下停在管家）。另 2 处是测试自己设计错（K7 没按「宽屏焦点卡是常驻栏」的既有设计判、K8 没先造出候选），已按既有设计改判 | 齿轮菜单补 WAI-ARIA 菜单按钮最小一组（键盘打开焦点进第一项、↓↑ 循环、Home／End、Esc 还焦点给齿轮钮、Tab 离开即收、按下一项即收 —— 带子浮层的两项除外）＋ `aria-controls`；`closeModal` 触发者不可见时把焦点还给「拥有这张菜单的按钮」；`.tool-tabs button.active:focus-visible` 焦点环叠在投影上（经典样式载荷锁按规程自证后重钉 f762f373… → 8f57fe64…）；`shell-mode.js` 接住被跳过过渡的 `ready`／`updateCallbackDone`，并给出「最后一次意图」`intendedShellMode()`，`app-frame.js` 的切换按它算 |
| `a11y-lint.browser` | 浏览器自己算的无障碍树（CDP `Accessibility.getFullAXTree`，读屏拿到的就是它）里可操作节点都有名字；重复 id；断 id 引用；非法 role；tabindex>0；嵌套可操作；aria-hidden 里可聚焦。专家档与简易档 × 管家／工作台／设置弹窗 | **0 条违规**（第 50 波 a11y P0 那批做得扎实）。首跑全绿的审计器可能是瞎的，所以**每一跑先往页面上种七条已知违规、逐条必须认出来**（尺子自检），再拔掉审真页面 | —（不下载 axe-core；这台小审计器的规则各对应 axe 的一条同名规则的核心判据） |
| `steward-shell-pixels.browser` | 管家壳深浅两张的格子基线（24×15，冻结动画、藏相对时间、等画面静止：连拍两张签名逐字相同才算） | —（新基线；两张 PNG 随基线入库供审阅） | 噪声实测：加「等画面静止」前 6 张图 1 张变 2 格（拍到轮询重画半途），加后连跑 3 次 6 张逐格 0 差。**反向**（改 CSS → 跑 → 还原核 sha256）：左栏 268→220 px 变 7／6 格、焦点卡换底色变 86／105 格、**一枚按钮换色**变 4／5 格 —— 上限定 3 格，三种都红。一格 60×60 px，比一枚按钮还小的改动仍可能漏（那是 DOM 断言的活） |

**产品修复的反向**（换回 HEAD 原文件 → 跑 → 按备份还原核 sha256，全部逐字一致）：旧 app-frame.js → K4／K5a 红、简易档 S4f 红；旧 tool-pane.css → K1 classic·dark 红；
旧 navigation-controls.js → K5（焦点掉 body）红；旧 shell-mode.js → K6b 两跑两红（`AbortError` 冒出、落点 steward）。

**真读屏（NVDA／讲述人）仍留给人**：这四件量的是键盘本身与浏览器算出来的名字，读屏软件怎么念、念得顺不顺，机器量不了。

**顺带一个读数（进 128f）**：热修线冒烟量到 **Full 包冷启动第一次 `/api/status` 要 5.4 s**（slim 0.28 s），冷缓存能力探测里桌面 python 探针那一段。

**128d 全量回归追记（`ff036d7`，4 路）**：`362 pass / 2 fail / 2 flaky`，两件真红都追到底了：

- **`walkthrough-round2` D6 是 128d 自己弄红的**。那一件（更早一波）钉着「从齿轮钮起连按 Tab，七项一个不落地依次拿到焦点」；128d 的齿轮菜单按 WAI-ARIA 菜单按钮模式做成
  「Tab 即收」，第一下 Tab 就把菜单收掉、焦点跑到左栏（实测走到 `railBoardBtn`）。**让步给先拍板的那一条**：Tab 在项间照走，焦点【走出】菜单（回到齿轮钮不算）才收起
  （`focusout` ＋ `relatedTarget`；为空时不动，点空白处由外部点击那条逻辑收）。`keyboard-walkthrough` 加 **K4g**（走的途中菜单开着、走出最后一项才收、焦点落到菜单外一个真控件上），
  与 D6 一起钉住两半。反向：换回「Tab 即收」→ K4g 红（走到 `railBoardBtn`）；拿掉 `focusout` → K4g2 红（菜单一直开着）。还原核 sha256 一致。
- **`ec-d-performance` 首次出现**（本会话此前 7 轮全绿）：首跑冷启动三样本 `658.9 / 1620.7 / 381.1 ms`（门 1500），重跑换了个样子 —— 浏览器调试端口 20 s 没出来
  （`CDP target unavailable`）。同一台机器上单跑三次全绿，样本 `~296–368 ms`。两次、两种症状都落在同一个 ~24 s 窗口里 —— 那一刻机器忙，不是本刀的前端改动（改动量对
  首屏可忽略，单跑数字与此前各轮同一量级）。**首次出现，记在这里**；按「跨轮重现就是缺陷」的纪律，再出一次就按缺陷取证（先查那一刻还有谁在吃 CPU）。
- flaky 首现两件（首跑失败、重跑通过）：`shell-session`（f1：`(Get-Location).Path` 回来的是**带提示符的回显行** `PS …\sess-cwd> (Get-Location).Path`，不是路径 ——
  像是读输出抢在命令执行完之前）；`steward-drawer`（E1／E1b／E1c／E3 一串：点兄弟页签没切到线程 B，后面全连锁红）。两件都是**第一次**进 flaky 名单，照录首跑 FAIL 行，
  再出现按缺陷查。

### 128f-① 用户首启走查：「首次启动显示连接错误、连接不上，但实际是连上了，新开个线程就好」（2026-09-19，主会话亲做；用户选「进 2.8.0」）

用户在另一台电脑上人机测试时报的。截图没拿到，追问后用户补了第二句：**「查看日志诊断似乎显示不出具体原因」** —— 这一句本身就是缺陷（见 ④⑥），也帮着定了位：
修前那张卡只有在 **`/api/status` 自己失败**时才看得见（那时视角 fail-closed 在工作台，卡画在工作台的对话区），而服务确实起着 —— 最像的是**首启时 `/api/status` 回了 500**，
诊断框里只剩一句被译掉的「服务发生内部错误。」。**哪一步、为什么，那一次的原文已经丢了**；下面每一处都是查出来的真缺陷，改完之后下次再出，卡上与日志里都会有原文。

**先排除的**（合成数据、隔离家目录、随机端口）：新 Full 包全新空家目录首启（无配置、无引导记录），`/health` 一通就开无头 Edge —— 105 个请求 0 失败、**不出故障卡**
（最慢 `/api/status` 2.7 s）；把「上次那条」的会话文件改坏／删掉再冷开 —— 也不出故障卡（会话层自己兜住了）。

| # | 缺陷 | 取证 | 修法 |
|---|---|---|---|
| 1 | **启动网络探针把在线的机器判成离线、缓存 60 s**（Brief §4.2 第 6 条「机制未证」的那一条）：探测进行中事件循环被同步活占住（启动期 detectDesktopMcp → pickPython 的 spawnSync，Full 包冷缓存要好几秒；热修线冒烟量到 Full 首个 `/api/status` 5.4 s），循环回来时计时器阶段先于 I/O，中止计时器先到期，早已到达的响应被判超时。这 60 s 里能力矩阵写「离线」、要联网的工具被滤掉、技能写「需要联网（当前离线）」、**提示词告诉模型「当前离线」** —— 过一会儿新开线程就好了 | **机制实验**（不改产品）：本地秒回的对端，探测中同步忙等 800 ms、超时 300 ms → **5/5 判离线**；对照：不阻塞 5/5 在线、对端不回话 5/5 离线 | `probeNetwork`：中止比约定晚到一个余量（250 ms）以上 ⇒ 量不准 ⇒ 记 `null`（未知）而不是 `false`；`probeAny` 任一 true 即 true、有「量不准」就整体未知；未知只缓存 5 s（`CAP_UNKNOWN_TTL_MS`）。`unit/capability-probe-stall` 五条；**反向**（只换回旧行为、常量照留，免得模块加载失败假红）→ S1／S4／S5 红，对照 S2／S3 照绿 |
| 2 | **桌面壳可能永远停在转圈页**：`RuyiDesktop.cs` 的 `MaybeNavigate` 在检查 WebView2 是否就绪【之前】就置 `navigated = true` —— 服务地址先到、WebView2 首启还没好（要新建 EBWebView 用户目录）时这一发空转，WebView2 好了再调又被挡住，窗口停在占位转圈页、**标题栏却写着「已连接」**。自 `6fdc28f`（8/8，桌面壳诞生）起就在 | 读码确认次序；没法在本机实演 —— 桌面壳的 WebView2 目录写死在真实 `%LOCALAPPDATA%\Ruyi`，跑它会动用户自己那份 | 只有真的发出了导航（或交给默认浏览器）才记 `navigated`；`desktop-shell.static` 钉次序与「就绪后补调」（反向：换回旧文件 → 红）；csc 编译到草稿目录核过 |
| 3 | **启动故障卡三处不对**：① **画在 `#messages` 里 —— 工作台视角的对话区，出厂默认的管家视角里整个不显示**：用户只看得到一个 2 秒的 toast「无法连接本地服务，请点『重试连接』」，找不到任何「重试」；② 对 boot 里任何一步抛错都说「连不上」＋端口／服务没起／安全软件三条原因，可大半步骤是在服务已经应答之后才跑的；③ 一抛就整段中止 —— boot 的最后一步是起推送连接，前面随便哪步抛了，推送就再也不起，界面能用却不再自己更新（新开线程时手动刷新才补上） | 读码确认；新件在管家视角里用 CDP Fetch 造四种失败 | 卡挂到外框之上的浮层（`#bootFailureHost`，z 58：压过弹窗与浮层、toast 照样在上）；传输层失败才说连不上，拿到了 HTTP 响应或前端自己的异常，说「本地服务是通的，但启动时有一步没走通」、不列那三条、按钮叫「重新加载」、多一枚「先继续用」；后者**不中止 boot**（语言、推送连接照走） |
| 4 | **诊断框「显示不出具体原因」**（用户第二句）：只有 `apiErrText(err)` 一行 —— 服务端 500 被按码译成「服务发生内部错误。」、它写在 message 里的真原因被扔掉（与 124 那次 `api.request_failed` 同一个模具）；前端异常只剩一句 message，不知道哪一步、没有栈；下面那行提示是半句「常见原因：」，后面什么也没有；按钮叫「打开诊断页面」却只是展开一块 | 读码；新件 B4／B5 | 诊断给一份能整段发给开发者的原文：**哪一步**（`bootData` 每步贴 `bootStep`）、哪一类、**状态码与路径**（`net.js api()` 抛的错误带上 `status`／`path`，全前端没有别处读这两个字段，已核）、服务端原话、错误码、栈（前端异常才给）、时刻（页面打开后几秒）、页面；多一枚「复制诊断」（剪贴板不给用时整段选中原文）；提示改成整句；`apiErrText` 对兜底码 `api.internal_error` 带上原话（「服务发生内部错误：ENOENT…」，过长截断） |
| 5 | **单花括号占位符永远不插值**：`interpolate` 只认 `{{name}}`，目录里有三条写成 `{name}` —— 点「重试」后状态行原样写着「已尝试 {n} 次」（还没传参）；智能体角色设置里旧值那一项写着「已保存：{value}」；一条没人用的 `{port}` | 普查四份目录 | 三条改成双花括号、重试传次数；`i18n.static` 加锁：四份目录里不许有单花括号占位符，唯一放行说明模板语法本身的那句（`playbook.create.fieldTemplate`，锁里自证它还真含一个 `{…}`） |
| 6 | **「看日志」里也看不到**：漏到路由顶层的异常只回一个 500，**结构化日志一个字不记** —— 用户在应用里看日志也找不到原因 | 读码（顶层 catch 直接 `sendError`） | 顶层 catch 记一条 `http_unhandled`：方法、路径（不带查询串）、状态、错误名、消息（截断）、栈前 6 帧。**SyntaxError 只留引号前那半句** —— Node 的 `JSON.parse` 会把请求体原文引进消息里，那里面可能有密钥（一发写坏了的配置保存）。`help-menu.e2e` ⑤b 钉（请求体以金丝雀开头，保证触发「引原文」那种消息）；反向：拿掉记录 → ⑤b 红；拿掉抹引号 → 金丝雀进日志、红 |

**本刀自己带出来、当场堵上的一条**：不再整段中止之后，`/api/status` 没到时 boot 会走到语言那一步 —— `state.config` 还是初值 `{}`，locale 读成 `'auto'`，
**会把按浏览器猜的语言写回配置、覆盖用户设的**（「写回没读到的状态」那个模具，与 118 设置保存、loadSession 同一家）。现在只有真读到了配置才写回；
重试拉起来之后按用户设的语言重应用一次（只应用、不写回）。B5b 钉「配置没读到的这一轮没有一发带 locale 的 `POST /api/config`」；**反向：拿掉守卫 → 实测 1 发**。

**结构**：卡整块从 `app.js` 搬进 `js/boot-failure.js`（工厂注入 `apiErrText`／`bootData`／`afterRecover`）—— 组合根行数护栏 D45（1280）本刀加完是 1391，
不抬护栏、把块搬出去，搬完 1243（比改前 1273 还少）。每一支都写字面的 `t('键')`（三元里套键名，键的使用扫描与 uimode-style (S2) 都认不出）。
新文件登记三处：离线覆盖包载荷表（`build-overlay.js`：缺它 `app.js` 的 import 整条挂掉、连这张卡都画不出来）、剪贴板白名单（`copy-path-guard`：复制的是诊断原文，不是路径）、
经典样式载荷锁按规程自证后重钉（8f57fe64… → 0b8c083a…；HEAD 检出重算与旧值逐字相同，改后 D51／F3 双红）。

**`boot-failure-kind.browser`**（新件，管家视角）：B1 连接被拒 → 连不上卡、三条原因、看得见、诊断写明传输层失败与步骤；B2 会话列表 500 → 「服务是通的」卡、在【管家】视角里看得见、
不列三条、诊断写明步骤／HTTP 500／路径／原话、提示整句、状态行不说连不上、推送照起、「先继续用」收得起；B4 兜底码 → 诊断有原话与错误码、说明行不再只剩译文；
B5 状态接口回 `null` → 我们代码里抛 TypeError、诊断写明界面代码出错／步骤／栈里有出错那一帧（`provider-settings.js:74`）；B5b 不写回；B5c 回的不是 JSON → 点名 SyntaxError；
B6 复制：交给剪贴板的就是原文、剪贴板不给用时整段选中；B3 重试：状态行「正在重新连接（第 1 次）…」、无生花括号、卡消失、左栏出现那条线程。
**反向**：`app.js`／`net.js`／CSS 换回 HEAD（目录保留新的）→ 17 条红，其中 **B2b「在管家视角里看得见」实测 `可见 false、命中空`** —— 服务是通的、某一步失败时，管家视角里用户只看得见那 2 秒的 toast（用户那一次看到了卡、点开了诊断，所以更像 `/api/status` 自己失败那一种，见上）；
拿掉写回守卫 → 只 B5b 红；兜底码只按码翻译 → 只 B4b 红。还原核 sha256 一致。

**一次事故，照实记**：B6 首版用真剪贴板验（`Browser.grantPermissions` ＋ `readText`）—— **新无头 Edge 用的是 Windows 系统剪贴板**（读回来多 4 个字符，正是 4 处换行被转成 CRLF），
那一跑把用户剪贴板里的东西换成了一段合成诊断文本。已改成页内桩（`navigator.clipboard` 换成收文本／拒绝两种），测试不再碰系统剪贴板。
普查过其余开浏览器的件：提到「复制」的七件都只是注释／文案，没有一件点复制钮；提到 clipboard 的其余件都不开浏览器。

**128f-① 全量回归追记（`fb3fadf`，4 路）**：`364 pass / 1 fail / 1 flaky`。
- 真红 `dom-contract`：动态建的 `#bootFailureHost` 不在静态 `index.html` 里 —— 有意为之（只在启动真失败时才建），按这把锁既有的规矩登记进 `DYNAMIC_ID_ALLOW` 并写明原因。
  快通道没抓到它，因为它不是 `.static` 件。热修线同改。上一轮的 `walkthrough-round2` D6 与 `ec-d-performance` 本轮都首跑即过。
- flaky：`classic-window-live-steer` **H8 第三次**在全量里红（128a 第二轮、128i、本轮），**这次红在 H8b**（前置：种下去的 `steer` 快照马上读回来是空的 —— 种和读之间页面自己又写了一次）。
  128i 装的诊断只在 H8f／H8h 红时触发，**这次什么都没留下**。补：给 `state.sessionRelay` 装 setter 探针（只记录、照原样存），H8b 红时打出每一次写入的时刻／当时是否在流／调用栈前三帧并保留夹具目录；
  自证：把种的值换成 `bogus` 强制 H8b 红 → 探针如实打出那一次写入、夹具保留（随后删掉这一次的目录）。单跑照绿。H8 仍按 128f 的一项继续取证。
- 同提交：master 的 CHANGELOG 2.8.0 节与发布线对齐（「第四处泄密面」与「首次启动误报『连不上』」两对中英条目，逐字取自发布线、插在同一位置）。

### 128f-② 右侧线程栏点兄弟页签会被看板下一拍拽回（2026-09-19，主会话亲做；**进 2.8.0 热修线** `e0c4d56`，master 同改）

**怎么追出来的**：`steward-drawer` E1…E3g 这一串在 128d 的 master 全量里首跑红过一次，HF2 发布线全量（`a26328d`，不含 128d）里又一次、签名逐字相同 ⇒
按「跨轮重现就是缺陷」取证，没有按偶发放行。件里装探针（页签条／标题每一次变化、`steward:focus-thread` 事件、点的那一下找没找到页签），
**6 路并发 × 3 轮 18/18 复现**，每一次都是：点中 B 的页签 → 抽屉开始读 B（「读取中…」、页签条清空）→ 约 175 ms 后被切回 A、页签 A 重新选中，期间没有任何 focus-thread 事件。

**根因（产品）**：宽屏下抽屉是右栏常驻那一份，看板每一拍 —— 轮询、每一条行推送、窗口切回来的 `visibilitychange` —— 都跑 `syncNow()`，
抽屉开的不是看板的焦点线程就 `drawer.openThread(焦点)`。页签点击（与 ←／→ 键）直接调抽屉自己的 `openThread`，看板不知道，于是下一拍拽回。
无负载时测试点完立刻判、看板那一拍还没来，所以只在负载下红；**真人碰到的形状**：点 B → 看一会儿 → 切到别的窗口再回来（或线程有动静）→ 回到了 A。
多半自 2.7.0 的常驻右栏（121-K6b）起就有。

**修**：页签点击与 ←／→ 改派 `steward:focus-thread`（全仓唯一的聚焦通道，看板钉住它、抽屉也听它）；没有 CustomEvent 的宿主退回直接打开。
**新断言 E1d**：切到 B 后派一次 `visibilitychange` 逼看板刷一拍，抽屉仍在 B —— 修前无负载 2/2 红（标题回到 A），修后 2/2 绿；负载 6×3 修后 18/18 绿（修前 0/18）。
E1 红时的探针诊断（点的那一下、页签条／标题时间线、前后 3 s 的 /api/ 请求）留在件里。

### 128f-③ `/api/status` 不再等桌面组件的 Python 探测（2026-09-19 下午，主会话亲做；设计见 §2-c）

**读数来路**：HF2 冒烟量到 Full 包首个 `/api/status` 8.9 s。追踪（`--require` 预载：同步三件、`ChildProcess.prototype.spawn`、fetch／http(s)／dns、事件循环卡顿）
定位到「不卡、在等」：请求期间事件循环几乎不卡，状态路由三处 `await ensureDesktopMcpWarm()`，那一趟是 Full 包内嵌 Python 跑 `from mcp.server.fastmcp import FastMCP`
（温 2.6 s，冷更久）。**追踪器自己踩过一个坑**：`execFile` 在 `child_process` 模块内调的是局部 `spawn`，只包 `cp.spawn` 看不见它 —— 包 `ChildProcess.prototype.spawn` 才全。
**另一个要照实写的**：真首启形状的追踪（空家目录）会让服务起真 Claude CLI 跑 `claude mcp add-json`（启动期 MCP 同步）；家目录环境隔离把写入落在临时家里 ——
事后核过用户真 `~/.claude.json` 修改时刻早于追踪数小时、零处提到追踪路径。

**修**（§2-c 四条判据逐条落）：`desktopMcpDetectionPending()` 用同一个 `detectDesktopMcp()` 在只读缓存模式下判「在飞」（`pickPython` 未命中只记一笔不探），
在飞就踢一脚预热；状态路由据此不等不探 —— `desktopMcp.probing`、健康项报既有的 `preparing`、`mcpConfigPath` 只回路径；缓存热时输出逐字段不变。
前端 `followDesktopMcpProbe()` 有界跟进（1.5 s × 至多 40），只换 `desktopMcp`／`health`／`mcpConfigPath` 三个字段、只重画桌面那一行（`renderDesktopMcpStatus`，
从 `fillSettings` 里单拎出来）与体检面板，不调 `fillSettings`。测试口 `WCW_TEST_DESKTOP_PROBE_DELAY_MS`；浏览器夹具加 `opts.serverEnv`。

**实测**：同一台机器、同一份解包的 Full 包、冷的 Python 缓存（TMP 指到新目录）—— 首个 `/api/status` **3194 ms → 214 ms**（最冷那一次是 8.7 s）。

**新件与反向**：
- `desktop-probe-status.e2e`（真服务子进程、合成桌面组件根、测试口延时 6 s）：P1 在飞时秒回且 `probing`（0.2 s）、P2 `preparing` 与路径、P3 `/health` 秒回、P4 探完后形状与改前一致。
  反向（路由照旧等探测）→ P1 实得 **5.4 s**，红。
- `desktop-probe-follow.browser`：D1 页面在探测在飞时起来、首个状态秒回；D2 那一行写「正在检测…」；D3 用户在另一个设置框里改了一半；D4 探完那一行自己更新；
  **D5 改了一半的框原封不动**。反向：跟进换成整页 `refreshStatus()` → **D5 红**（框被冲成空串）；不起跟进 → D4 红。
- **测试口第一版放错了地方，照实记**：延时先放在 `detectDesktopMcpAsync` 开头，于是热缓存的预热也要等 —— 状态路由非在飞路径连等三趟、每次 15 s，把「整页回填」
  那个反向弄成了 D4 超时红（红的理由是错的）。挪到 `pickPythonAsync` 缓存未命中之后（只在真要探时等，与真机同形），三个反向都按预期的那一条红。
- 锁跟上：health-i18n 两处源码锚点改按函数头前缀认（签名多了参数）；墙钟窗口 21 → 22 件（两处就地豁免、写明界是延时的一半）；fixture-home 151 → 152；
  process-safety 253 → 254；README 374／367。快通道 76/0。

**不在本刀**（照旧登记）：启动时 `detectClaudePath`／`detectKimiPath` 同步 `--version`（~1.7 s，卡在 listen 之前）；Python 探针本身换更轻的判法。

**128f-③ 全量回归追记（`193d28a`，4 路）**：`366 pass / 1 fail / 1 flaky`。
- 真红 `boot-listen-budget`「desktopMcp 字段形状不变」：它钉的正是本刀有意改掉的旧口径（「首个 /api/status 付一次探针」）—— 探测在飞时
  那一发现在秒回并多一个 `probing` 键。改成认两种形状（探完原样三键；在飞时三键＋`probing:true` 且 `detected`／`resolved` 为 null），头注 ② 同步改写。单跑绿。
- flaky：`classic-window-live-steer` **H8 第四次**，仍红在 H8b。上一刀装的 setter 探针**这次的证据还是丢了**：run-all 对 flaky 只留首跑的 `FAIL` 行，
  重跑通过后首跑的夹具目录也跟着删 —— 探针打出的 `H8-DIAG` 行不在 `FAIL` 开头，一行没留下。修：`failLines` 同时收 `<名>-DIAG` 开头的诊断行
  （每行截 2000 字、合计至多 20 行），H8／E1 这类「红时就地留证据」的件下一次在全量里红，证据会出现在汇总的 `[首跑]` 里。

### 128f-④ 从工作台切回管家，焦点卡标题不再闪「读取中…」（Brief §4.2 第 23 条；2026-09-19 傍晚，主会话亲做）

抽屉离开管家视角即收摊，回来 `openThread` 把手上的一切清空、落 117k 的「读取中」闸再整份重取 —— 同一条线程每切一次视角就闪一次。
117k 的闸防的是「还没读到就说假话」；重开**同一条**时手上有它刚刚还是真的那一帧。**修**：收摊时记下这一帧（线程、会话、事项行、快照、待决、标题；
活回合尾巴不记，它是瞬时的），5 分钟内重开同一条就先画它、不落闸，后台照常 `refreshOnce`；正开着的那一条被再次「打开」（如点它的「打开」）同样不清空。
换到别的线程与头一次打开照旧落闸。

**第一版漏了一处，件当场抓到**：离开管家视角时收摊会**连来两次**（看板 `syncNow` 一次、抽屉自己的视角观察者一次），第二次手上已经没有线程，
`lastFrame = captureFrame()` 把刚记下的那一帧冲成了 null —— 切回来照闪。改成「记到了才覆盖」。

**新件 `steward-drawer-keep-frame.browser`**：K0 A 在焦点卡；K1 切到工作台、期间把 A 改名、切回管家 —— 标题变化序列里一次「读取中…」都没有、第一帧是上一帧的旧名；
K2 后台刷新到了（最终是新名字）；K3 对照：从没开过的 C 照旧先落「读取中…」闸。**反向**：换回 HEAD 的抽屉 → K1 红（序列以「读取中…」开头），K3 照绿；
「空值冲掉记下的帧」那一版 → K1 红（就是第一版）。既有的 `steward-drawer`、`one-workbench-frame`（K6 与读取闸那一条）、`workbench-thread-head`、`quiet-card` 单跑全绿。
README 375／368。快通道 76/0。

### 128f-⑤ 模型菜单首开不再挪位（Brief §4.2 第 21 条；2026-09-19 傍晚，主会话亲做）

修前：模型菜单**第一次打开才去拉**用量（`/api/usage/summary`），约 140 ms 后到货、「常用」那一段插进列表顶部、整段下挪约 43 px。124 起只在
「指针悬停在菜单里／焦点落在某一行」时不重画 —— 挡得住鼠标，**挡不住没有悬停的触屏／笔与键盘用户**：他们手指／光标底下那一行变了，会点错。
**修**：① 用量在**有意图**的那一刻就去拉 —— 模型 chip 的 `pointerenter`／`pointerdown`／`focusin`（仍是一次页面生命周期一趟、零计时器）；
② 开着的菜单**一律不因用量晚到而重画**，晚到的「常用」下一次打开再画。于是鼠标（先悬停）与键盘（先聚焦）第一次打开就有「常用」，
「一下点开」的那一次晚一次、但绝不挪。代价照实写：124 那条「候选 > 8 的菜单第一次打开也要看到常用」在「一下点开」时不再成立（由预取兜住大多数情形）。

**新件 `model-menu-no-shift.browser`**（盘上种这条线程所用模型的用量；CDP Fetch 扣住用量回包）：N1 一下点开、用量在菜单开着时才到货 ——
每一行位置与分段标题一个都不变；N2 关上再开「常用」出现（用量确实到过）；N3 新页面里指针先进到 chip 上，用量在点开之前就回来，第一帧就有「常用」。
**反向**：换回 HEAD 的 `steward-chips.js` → N1 红（最后一行 315 → 336 px）、N3 红（没有预取）。
**旧件跟着改**：`steward-drawer` M2 与 `unit/steward-model-menu` 原先靠「开着的菜单等用量到货重画」—— 改成真人的路径（先 pointerenter、等那一发回来、再点开），
假 DOM 学会记监听与 `dispatch`；新加 M1c 钉「指针进来就去拉」。`walkthrough-round1`（它的 F 段靠「指针进菜单后不重画」）照绿。README 376／369。快通道 76/0。

**128f-⑤ 全量回归追记（`7bac055`，4 路）**：`369 pass / 0 fail / 1 flaky`，退出码 0。那一件 flaky 是 **H8 第五次**，**这次证据带回来了**（setter 探针 ＋ run-all 的 DIAG 行）：
`H8-DIAG relay writes` 两条、**同一毫秒** —— ① 测试自己种下 `steer`（`streaming:false`）；② 紧接着被 `steerPrompt`（`chat-stream-runtime.js:843`）清空。
那一行是 124 的自纠正：插话收到 `steer.no_live_turn` → 作废「可以插话」的信念、改按普通回合重发。**产品没错，是测试的前置没立住**：
负载下走到 H8 时这条线程上**上一段的回合还活着**（多半是 H8pre 关管家之前管家刚起的那一个），① 那一下「发送」被路由成插话，H8a 看到的 streaming 是上一个回合的；
插话落地时那个回合已经收尾 → 自纠正 → 清空的恰好是 ② 刚种下的快照。
**修（只改测试）**：起 ① 之前等线程真的空着（服务端 GET 信封没有 relay、本页不在流，H8pre2），并把本页的插话信念归零；① 之后钉「盘上多了一条普通用户消息 YY8-run」（H8a2）——
「信念过期时发送也要发得出去」那条保证仍由 ④⑤ 用种进去的快照专门钉。单跑绿；负载 6 路 × 2 轮 **12/12 绿**。
**照实写**：没能确定性地构造「上一个回合恰好在点下与插话落地之间收尾」那个形状（假 provider 只有带 `SLOW` 的消息才慢，① 那一发本身秒回），
所以这一刀的依据是「探针钉住的机制 ＋ 修法拿掉了机制所需的前提」，没有修前强制红的对照；再出现一次，DIAG 行会进汇总。

### 128f-⑥ 用量账本退出前补写（Brief §4.2 第 5 条「账本即发即忘」）＋ 128f-⑦ 管家看到的命令不再被中和改写（第 7 条后半）（2026-09-19 傍晚，主会话亲做）

**⑥**：`appendUsageLedger` 是同步入口 ＋ 内部 promise 链，全仓调用点无一 `await`。实际会丢的只有一种形状：`serve` 的 SIGINT／SIGTERM／未捕获异常三条收尾
都走 `process.exit`，链上还没轮到的那几行就丢了（`mcp` 子进程是事件循环自然排空退出，链会跑完；被硬杀谁也救不了）。
**修**：排队的行登记在 `usageLedgerPending`（queued），`serve` 既有的 `exit` 监听器（02 的 `flushSessionIndexSync` 同一处）里 `flushUsageLedgerSync` 用同步 I/O 补写 queued 的；
**正在写的那一行不补**（落没落盘不知道，补了可能记两遍，费用翻倍比少一行更糟）；链上轮到已被补写的行就跳过。
新单测 `unit/usage-ledger-exit-flush`：**L0 修前读数**（子进程追加一行后立刻 `process.exit`、不补写 → 那一行**丢了**）；L1 挂补写 → 在、只有一行；
L2 连排两行立刻补写 → 各一行、链跑完不重复；L3 正常路径补写返回 0；L4 正在写的那一行不补。**反向**：链不看状态照写 → L2 红（记两遍）。

**⑦**：管家的尖括号中和（`stewardSanitizeText`／`stewardSanitizeBlock`，喂给管家的外来文本不许拼出它提示词里 ASCII 的围栏标记）修前换成方括号 ——
`2>&1` 变成 `2]&1`、`a < b` 变成 `a [ b`，管家读代批的命令摘录时意思就变了。**改换全角 ＜＞**（U+FF1C／U+FF1E）：照样拼不出 ASCII 围栏，人和模型都还读得出是重定向／比较。
四件单测与 `steward-tools.static` 里钉着旧映射的期望跟着改（判据不变：没有 ASCII 尖括号、可读文本留着），另加一条正面判据「`cmd 2>&1 | findstr a<b` → `2＞&1`、`a＜b`」。
`prompt-snapshot` D14c 钉的是另一处中和（108b 的 playbook 不可信描述，仍是方括号），本刀不动它。
**第 7 条前半（管家拒代批后线程干等 120 s 再问 600 s）没有在本刀修**：它是「needs_you 没有事件唤醒」那条债的真机形状 —— 线程该等人多久、拿什么叫醒人，是产品选择，列给用户。

锁：README 64 组 unit suite；快通道 76/0。

**128f-⑥⑦ 全量回归追记（`9f31209`，4 路）**：`366 pass / 3 fail / 0 flaky`。三件真红**全是 ⑦ 我漏改的旧期望**：`steward-exempt-no-swap`（P1／P2b 两处 `[/exempt-command]`）、
`steward-signals`（G6 `\[docs\]`、G7 等插话消费的正则 —— 等不到就超时，所以是 121 s 的红）、`steward-tools`（D7 `[one-pager]`）。改 ⑦ 时只扫了单测与一件静态件，
**e2e 里用正则字面量（`/\[docs\]/`）与跨两行的断言没扫到** —— 按行 grep「中和＋方括号」是漏的。五处改成全角 ＜＞（判据不变：没有 ASCII 尖括号、可读文本留着），三件单跑全绿。
H8 这一轮没有出现。
**`33d58ac`（补这五处）全量**：`369 pass / 0 fail / 0 flaky`，退出码 0，残留测试浏览器 0。

### 128f-⑧ 会话索引扫盘重建的竞态 ＋ 用量页标题读旧值（Brief §4.2 第 22 条；2026-09-19 晚，主会话亲做）

**前半 · 重建竞态**：`listSessions` 在索引不在／坏了／id 集合与磁盘对不上时走扫盘重建 —— 一条条读会话文件、最后整份写回索引。
扫到 X 之后 X 又被存了一次，而那次存的索引刷新先落了地（或者索引此刻不在，`flushSessionIndex` 见 `!index` 直接把那一批丢掉），重建写回的就是 X 的**旧值**；
之后 id 集合与磁盘对得上，快路径一直信它，**直到 X 再被存一次**。
**修**：每一次排进索引的写（`scheduleSessionIndexUpdate`）记一个全局序号；**有重建在扫时**再记下每个 id 最新一条的值（`sessionIndexLatestWrite`）。
重建开扫前记下序号，写回时在同一把索引锁里把「开扫之后的写」一条条叠上去（墓碑删掉），返回值也用叠过的那份。
那张表只在有重建在扫时记、最后一个重建收尾时清空 —— 否则它会随会话总数只增不减（初稿就是这样，落刀前改掉）。
**后半 · 用量页标题**：`buildUsageSummary` 的 `bySession` 标题修前直读盘上索引；索引写是去抖 ~200 ms 的，刚改名的会话在这一窗里还是旧标题。
改成与 `listSessions` 同一个叠法（`overlayUnflushedSessionIndex`：盘上 → 在飞 → 排队）。

**新单测 `unit/session-index-rebuild-race`**（确定性构造，不靠跑 N 次）：测试口 `setSessionIndexRebuildScanHookForTest` 在扫盘每读完一个文件时调一次，
R1 在扫到 X 之后改名存一次并立刻刷新（索引此刻不在）→ 第二次 `listSessions` 必须是新名，且收尾后那张表为空；
R2 改名刚存、还在去抖窗口里时 `buildUsageSummary('all')` 的标题就是新名；R3 对照：没有中途保存的普通重建不多不少，且没有重建在扫时的保存不进那张表。
**反向**（改构建产物，备份还原、sha 核对一致）：重建不叠 → R1 红 `'r1-2' !== 'renamed-mid-scan'`（R3 跟着红：快路径继续给旧名、重建给真名 —— 正是这条债的形状）；
用量页换回直读 → R2 红 `'usage-old' !== 'usage-new'`；不在扫时也记 → R3 红（1 条，应为 0）。

锁：README 65 组 unit suite；快通道 76/0。

**128f-⑧ 全量回归追记（`c3a3585`，4 路）**：`369 pass / 0 fail / 2 flaky`，退出码 0，残留测试浏览器 0。两件 flaky 都按「偶发即缺陷」取证，见 128f-⑨。

### 128f-⑨ 两件 flaky 的取证：H8 第六次（测试前置）＋ `workbench-thread-head` E3（**顺藤摸出工作台视角五处产品缺陷**）（2026-09-19 晚，主会话亲做）

**H8 第六次**：DIAG 行把机制带回来了 —— 同一毫秒里先是测试种下 `steer`（`streaming:true`），紧接着 `sendPrompt` 回合末作废（`chat-stream-runtime.js:729`）把它清空。
① 那一发（「慢慢想一会儿 YY8-run」）在假 provider 里是**秒回**的，负载下 ① 恰好在 H8a2 与 ② 之间收尾 —— 测试以为「② 时回合还活着」，其实是赌的。
第五次是「上一段回合还活着」，这次是「这一段回合已经没了」，同一类：**测试假设了它不控制的回合寿命**。
**修（只改测试）**：假 provider 认 `HOLD8`：回合第一发先流一句、挂住，直到测试放行（H8b 之后；收尾兜底也放行，另有 60 s 上限）。新增 H8b0「种 ② 的那一刻 ① 还在流」。
**反向**（强制形状）：在种 ② 之前放行、等 1.5 s → H8b0 绿、H8b 红，DIAG 与第六次**逐字同形**（`steer`@streaming:true → `sendPrompt:729` 清空）。单跑全绿。

**`workbench-thread-head` E3**（首次出现；它在全量里是独占段跑的）：给测试加就地留证（E3-DIAG），负载 6×3 复现，复现里**一共四种红**，逐一取证 —— **全是产品缺陷**：
1. **E3 —— 管家条跟不上在场**：DIAG 实测**服务端那一行 `seatedBy:'user'`、界面仍是「如意盯着这条」**。行上的 `seatedBy` 是服务端按在场现算的，在场只在事件流连上那一刻登记
   （换会话／换视角＝重连，去抖 300 ms）；在那之前取回来的行说「没人坐着」，而**工作台视角没有兜底节拍**（`syncPolling` 只在管家视角起表）、**在场变了服务端也不推** ——
   那一句就一直错下去（测试等了 32 s）。另一半：推送那一路（`pushRefreshRows → refreshRows`）行变了只重画左栏，**从不告诉宿主**（`onRowsChanged` 只在 `refreshBoard` 里），
   而线程头读的也是这批行。**修**：`steward-board.js` 收到 `presence.ack`（服务端「我已经记下你坐在哪」的回执）补一发 ETag 化的行；`refreshRows` 行变了照样调 `onRowsChanged`。
   只管本页自己的在场；别的窗口换座不在此列（服务端不广播在场，照实记）。
2. **C1＋D1 成对红 —— 打开会话后发先至（最严重）**：开机那一发 `openSession(B)`（有回合在飞的那条）负载下还没回来，测试已经点了 C；C 先回来、页面画成 C（测试的等待通过），
   B 晚到又把 `state.currentSession` 与标题写回 B —— **线程头与 chip 绑在 B 上，用户以为在改 C 的模型，PATCH 落在 B**（C1），面包屑是 B 那件事的（D1）。
   **修**：`session-experience.js` 的 `openSession` 取序号，回来时已经不是最新那一次就整个丢掉；`newSession` 也取号。
3. **A11 —— 修 2 之后第二轮负载复现冒出来的：开机「打开上次那条」按序号盖过用户那一下**。`openSession` 要等 GET 回来才写 `wcw.lastSession`；用户点 C 的那一发还在飞时，
   开机那一段读到的「上次那条」仍是 B、发出「打开 B」—— 只有序号时它比用户那一下「更新」，于是赢。修前（没有序号）是谁的回包晚谁赢，同一个洞的另一副面孔。
   **修**：开机那一发带 `restore:true` —— 它是缺省不是选择：此前已经有过一次真的选择（非 restore 的打开／新建）就整个让路。
4. **E4c＋E4 成对红 —— 开关的第二下被静默吞掉**：E4C-DIAG 实测**第二下零 PATCH**、开关弹回「没盯」。`setWatch` 的「忙」一直占到写完之后那一发**读**（重取行）也回来，
   负载下那一发要几秒，这几秒里再点一下被吞，原生复选框自己翻了面、随后那次 render 又翻回去 —— 用户看到的是「点了没反应」。
   **修**：「忙」只挡并发的写，PATCH 一回来就放开；被挡下或写失败的那一下立刻按行重画。
   **顺带同一类**：`loadMissions` 同时可能有好几发在飞（开关、在场回执、推送、兜底），先发的晚到会把新行与 ETag 盖回旧的 —— **只认最后发出的那一发**
   （后发的那一发看到的服务端状态不会更旧：同一个 If-None-Match 出发，304 说明没变，200 则至少一样新）。
5. **修 4 的副作用，第三轮负载复现逮到**（10/12 绿，剩下两件 E4c 换了签名：第二下**发了** PATCH、服务端却停在 false）：被取代的那一发**立刻回 false**，
   而 `setWatch` 紧接着 `render()` —— 画的是两发都还没落地时的旧行（watched 仍是 true），开关被勾回去，下一下点击于是发出的是 false。
   **修**：被取代的那一发**等最后那一发落地再回**（`loadMissions` 记住最后一发的 promise）。先在新件里把这个交错**构造出来**（W2，修前红、记录里出现「…|true」），再改。

**新件 `thread-switch-race.browser`**（CDP Fetch 扣包，形状是构造出来的，不靠负载去赌；五段）：
T1 扣住 B 的 GET、点 B 再点 C、C 画出来之后放行 B —— 页面仍是 C；
S1 测试从服务端把 B 交给管家盯（推送走现有连接当场落地，之后就没有可补发的行事件）、扣住事件流再点 B —— 前置成形（服务端行上 B 没人坐、管家条「如意盯着这条」），放行 —— 换成「……你坐着时它不动手」；
W1 取行在**回包**阶段扣住、点开关、再点一下 —— 第二下必须发出 PATCH；W1e 放行那几发带旧数据的取行，管家条一次都不许被盖回去（MutationObserver 记每一次变化）；
T2 扣住开机的会话列表与用户点 C 的那一发 GET，放行列表（开机读到「上次那条」是 B）、再放行 C —— 页面仍是 C；
W2 先扣事件流、再扣取行回包，点开关 → #A 扣住；放行事件流 → 补发的行事件与在场回执发出 #B（扣住）；只放行 #A —— 开关不许拿旧行画一帧「勾着」。
**反向**（逐条备份还原、sha 核对一致；七条各红在对的地方）：`openSession` 不取序号 → T1 红（页面被写回 B；S1 连带）；不收 `presence.ack` → 只有 S1 红（前置 S1g 绿）；
`refreshRows` 不告诉宿主 → 只有 S1 红；「忙」占到读回来 → W1 红（服务端 watched 停在 false）；开机那一发不让路 → 只有 T2 红（页面是 B）；
`loadMissions` 不认最后一发 → 只有 W1e 红（记录里出现「如意看得见、不插手|false」）；被取代立刻回 false → 只有 W2 红（记录里出现「…|true」）。后两条另有静态锁 F3e 同红。
**反向验证先逮住了新件自己的洞**：T2 第一版只扣列表不扣 C 的 GET —— C 先回来写了 `wcw.lastSession`，开机读到的已经是 C，拿掉「让路」照样绿；改成两处都扣才造出 A11 的形状。
锁：`steward-board.static` F3c 的 `pushRefreshRows()` 处数 4→5，新增 F3d 钉住「在场回执补一发」与「refreshRows 告诉宿主」、F3e 钉住「只认最后一发、被取代的等它落地」。
`workbench-thread-head` 另加两处就地留证（E3-DIAG／E4C-DIAG，PATCH 的 body 另记一份给 DIAG，`posts` 的形状不动）。README 377／370。
**这一趟的教训**：负载复现不是跑一轮就完 —— 修一处，下一轮换一副面孔（C1／D1 → A11 → E4c 两种签名），每一副都要先在确定性件里造出来再修；
第三副是自己上一刀的副作用，没有第三轮负载复现就会带着它出门。
负载复现（6 路并行，比全量里的独占段重得多）：`workbench-thread-head` 修前 6×3 **6/18 绿**（后两轮途中已开始改码，只作取证用）→ 修 1–2 后 6×2 **1/12 绿**（E4c 旧签名 9 件、A11 2 件）→ 修 3–4 后 **10/12**（E4c 新签名，即 5）→ 修 5 后 **12/12 绿**；新件 6×1 **6/6**；`classic-window-live-steer`（H8 闸门）6×1 **6/6**。
