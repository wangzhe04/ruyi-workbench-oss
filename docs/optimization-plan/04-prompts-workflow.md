# 04 · 提示词优化与模型工作流程规范化方案

> 对应所有者确定方向 4。核心判断：**功能上提示词体系已相当完备（分层、围栏、防注入、按需装载俱全），欠的是工程化——外置、i18n、缓存稳定、预算闸、评测。改动提示词是行为漂移高风险操作，必须先建护栏，再动文本。**

---

## 1. 现状盘点

### 1.1 提示词体系全貌

**Provider 引擎**（`buildProviderSystemPrompt`，`06-provider-engine.js:1195-1335`）名义"四层"实为约 10 层顺序拼接：

| 层 | 内容 | 预算 | 证据 |
|----|------|------|------|
| 身份 | provider.label+model（刻意不含产品名） | — | 06:1202 |
| 工具协议守则 | 先读后改、tool_search 按需装载、现成工具优先于脚本 | — | 06:1207-1214 |
| 能力层 | 联网/桌面工具数/git/ripgrep + 「当前不可用」清单 | 易变 | 06:1221-1247 |
| 桌面操控规程 | 视觉/文本双路径 + Office 产出禁令 | — | 06:1249-1275 |
| 主动检索 | — | — | 06:1283-1289 |
| 风格 | — | — | 06:1294 |
| 项目层 | CLAUDE.md 围栏 | ≤16KB | 06:1299-1305 |
| 技能层 | `<skill-index>` | ≤3000 字 | 06:1311-1314 |
| 记忆层 | `<workbench-memory>` | ≤2000 | 06:1318-1321 |
| 任务账本层 | `<mission-ledger>` | ≤1200，易变 | 06:1324-1327 |
| provider 自定义 | append | ≤8000 | 06:1331 |

主回合再叠加：角色清单（09:909）、编排模板提示 `buildOrchestrateHint`（08:1313-1318）、模型选型提示（13:1564）、plan 模式指令（09:929）、团队模式+响应语言政策尾（06:1097-1106）。不可信内容一律围栏 + 伪造围栏字符中和（06:1171/1348；07:241/263），截断保围栏闭合（`fenceSafeSlice`/`shrinkFencedSection`，06:1371-1411）。

**Claude 引擎**：CLI 自建 prompt，工作台经 `--append-system-prompt` 注入 append+MCP 提示+浏览器提示+账本 digest+政策尾（05:138-204）；技能/记忆/编排索引走 stdin `<workbench-context>` 一次性注入 + hash 去重（05:207-235）；cmd8191 命令行预算防线含降级阶梯（05:115-130/248-264）。

**辅助提示词**：压缩摘要四段式（10:251-256）、playbook 起草（06:911-925）、JSON 修复器（06:994）、质量门指令（09:459-460）。

### 1.2 模型工作流程现状

- **预算**：`TOOL_ITERATION_BUDGETS` 100/200/300/50（06:1114）；`isLongToolTask` 关键词启发式升档（06:1119-1128）；按进展事件动态扩展（06:1147-1151；09:1102-1113）。
- **循环保护**：主/子回合同签名连击 warn@3/abort@5（09:1023-1029/1322-1347；08:666）；DAG 节点 progressFingerprint + noProgressLimit（09:609-621）；Mission 停滞 K 轮降级（10:584-592）。
- **plan 模式**：首消息 `PLAN:` 暂停-批准-仅当回合放行（09:1168-1232/1418）。
- **检查**：质量门 schema + verdictPasses + vote/dedupe 确定性聚合 + JSON 修复管线（08:881-887/1045；09:491-503；06:992-1020）。
- **上下文治理**：窗口三级解析 + 超限学习只降不升（10:56-72）、EMA 自校准（10:107-124）、L1 蒸发 / L2 摘要重播种（10:490-543）。

### 1.3 问题清单

| # | 问题 | 证据 | 影响 |
|---|------|------|------|
| G1 | **提示词全中文硬编码、无 i18n**：Provider 系统层（06:1202-1274）、plan 指令与 loopWarning（09:929/1523）中文；Claude 引擎 hint 段却是英文（05:143/146）——中英混杂，回复语言只靠政策尾补救 | 06/09/05 | 开源英文用户短板；双语用户得到混杂指令 |
| G2 | **易变层放在 system 首条，自破 prefix cache**：账本 digest、能力层每轮可能变化且位于会话最前；10:209 注释自述"append-only/缓存经济学"但结构违背它 | 06:1324、10:209 | 每轮全量重算前缀，成本与延迟双损 |
| G3 | **Provider 侧系统提示无总预算闸**：16KB+3000+2000+1200+8000 可叠加超 25KB，无 Claude 侧 cmd8191 式整闸 | 06:1195 全函数无总长校验 | 小窗模型被挤爆 |
| G4 | 拼装点分散在 06/09/13 三处，无单一注册点 | — | 版本化/A-B 实验无从做起 |
| G5 | loop-guard 只认同签名连击：参数微调的语义死循环（换路径反复读同类文件）不触发；主回合无 DAG 节点的语义进展指纹 | 09:611 仅节点级 | 死循环漏网，烧钱 |
| G6 | 引擎能力不对称：steer/plan 流程仅 provider 侧；Claude 子代理角色 prompt 不注入（只用 role 的 tools/models/mcpServers，职责文本不进会话） | 13:1227；07:1722-1752 | 团队模式 Claude 节点"有角色名无角色魂" |
| G7 | 坏味道：`buildMissionPromptSection` 三元两分支相同死代码；预算钳 [1,200] 与 hardLimit 300 口径不一；`isLongToolTask` 关键词正则脆弱 | 07:264；06:1132 vs 1139；06:1123-1127 | 维护噪音、误判升档 |
| G8 | 无提示词快照测试：改提示词全靠人工回归 | dev-harness 有行为锁但无文本快照 | 改动风险不可控 |
| G9 | 常驻 schema 臃肿：`orchestrate_agents` 单条 description 约 700 字符常驻 | 13:2149-2213 | token 成本 |
| G10 | 模型工作流"规范"只存在于代码与内部文档，无用户可见的《工作流规范》文档（规划-执行-检查-循环保护-预算的约定） | — | 高级用户/贡献者无从遵循 |

## 2. 实施策略：先护栏，后文本，再机制

### Phase A · 评测与护栏建设（W46，先于一切文本改动）

1. **提示词快照测试**：对 `buildProviderSystemPrompt` 在固定假配置下的输出做分层快照（每层单独断言 + 总长断言）；Claude 侧对 `--append-system-prompt` 拼装与 stdin `<workbench-context>` 注入做同款。快照即"改动清单自动生成器"——任何提示词 diff 必须体现在快照更新里，review 可见。
2. **请求体落盘断言扩展**：`FAKE_CAPTURE_DIR` 范式（dev-harness 已有）推广到压缩摘要、质量门、playbook 起草、JSON 修复器四类辅助提示词。
3. **跨模型 A/B 评测夹具**：以 `model-tier-probe.js` 为底，建"提示词基准集"——10–20 个代表性任务脚本（工具使用规范、先读后改、Office 禁令、loop 自救、plan 触发），改提示词前后各跑一次对比通过率。i18n/README.md:99 已立原则"模型内容本地化须版本化、回归评测、安全审查"，提示词复用同款纪律。
4. **预算断言**：系统提示总长闸（见 Phase C）+ 每层预算的静态锁。

### Phase B · 提示词外置与 i18n（W47–W48）

1. **外置为资源文件**：散在 06/09/13 的提示词文本抽为 `app/prompts/` 目录下的结构化文件（按层一文件，中英双份），构建期随 manifest 拼接或运行期按 `config.locale` 加载；代码里只留拼装逻辑。沿用 locale 文档副本同步惯例与"双语键同交"lint 纪律。
2. **单一注册点**：`prompts/registry.js` 导出全部层 + 拼装顺序声明，`buildProviderSystemPrompt` 瘦身为纯装配器；版本号注入（`PROMPT_PACK_VERSION`），会话元数据记录所用提示词包版本——为 A-B 实验与问题回溯奠基。
3. **中英语义对齐审校**：不是逐字翻译，而是逐层双语等价（工具协议守则、桌面规程这类行为关键层优先）；英文母语级润色。
4. **坏味道顺手清**：G7 死代码与口径不一致修复。

### Phase C · 结构与缓存优化（W48）

1. **系统提示稳定化分层**：system 首条只放逐字节稳定层（身份/工具协议/桌面规程/风格）；易变层（账本 digest、能力层、当日日期等）移到 user 侧尾部消息或回合首条 user 消息的前缀块。保住上游 prefix cache（对长会话是实打实的成本与延迟优化）。注意验证各 provider 对多 system/消息序的兼容性，fake-openai 剧本补断言。
2. **Provider 侧总预算闸**：仿 cmd8191 思路做逻辑预算——总长超阈值时按层优先级降级（项目层截断 → 技能索引瘦身 → 账本 digest 化），并记录降级事件供 UI 展示（压缩🗜事件族已有先例）。
3. **工具 description 瘦身**：`orchestrate_agents` 等超长常驻描述压缩，细则挪到 tool_search 按需返回（06:1209-1211 的按需装载机制已有，把"描述细则"也挂进去）。
4. **技能/记忆注入策略复核**：技能 181 个 i18n key 是最大前缀，索引 3000 字预算在小窗模型下是否再分级，结合 EMA 校准数据（`context-calibration.json`）评估。

### Phase D · 模型工作流规范化（W48）

1. **语义级 loop-guard**：在"同签名连击"之外增加"结果指纹无进展"判定——工具结果摘要 hash 连续 N 次无新信息即 warn；把 DAG 节点的 progressFingerprint 思路推广到主回合（09:611 已在节点级验证）。误报防护：read/search 类探索工具放宽阈值。
2. **预算口径统一**：用户钳 [1,200] vs hardLimit 300（06:1132/1139）拉齐；`isLongToolTask` 关键词启发式升级为"首轮工具使用模式再判定"（首轮即大量 exec/read 自动升档），减少开局误判。
3. **回合级输出契约**：目前仅 DAG 节点有 outputSchema/质量门；给主回合引入轻量 self-check 约定（长任务收尾时输出"完成声明：做了什么/没做什么/验证方式"），先提示词约定后机械校验。
4. **Claude 引擎对齐**：角色 prompt 注入补齐（07:1722-1752 的 role 职责文本经 `--append-system-prompt` 或 stdin 注入子代理会话）；plan 模式差异写进能力对齐表（Claude 走 CLI 自带 plan mode）。
5. **《模型工作流规范》文档**：面向用户与贡献者写清"规划-执行-检查-循环保护-预算-压缩"的工作方式与各档预算语义，挂到 ruyi-workbench/docs/；规范即营销素材（"可审计的工作方式"）。

## 3. 风险与缓解

| 风险 | 缓解 |
|------|------|
| 提示词改动引发模型行为漂移（工具误用/loop/拒答） | Phase A 护栏先行：快照 + A/B 基准集，任何文本改动过评测才合入 |
| 消息层移动（Phase C）破坏 provider 兼容性 | fake-openai 剧本 + 各主流 provider 抽测；失败回滚为原结构 |
| 双语提示词语义分叉 | 逐层等价审校 + 双语快照对照；行为关键层以中文原版为准绳翻译 |
| 语义 loop-guard 误伤正常探索 | 探索类工具宽阈值 + warn 先行不 abort + e2e 覆盖"换路径多读"场景 |
| 外置资源文件加载失败 | 构建期内联兜底或启动自检（syntax-gate 同款思路），缺文件拒绝启动并明示 |

## 3.1 2026-08-11 增量：任务路由的软件工程策略包

已在 `06b-prompt-registry.js` 增加中英双语 `softwareEngineering` 策略包，并由
`softwareEngineeringTaskProfile` 对代码、仓库、测试/构建和 Git 任务确定性触发。普通问答不注入，避免把
仓库规程常驻到所有对话；命中后按任务组合 scope、preflight、implementation、debugging、verification、git、
completion 七个模块。

- 主 Provider 与 Claude 回合均基于当前用户消息选择策略；OpenAI 子代理与 Claude DAG 节点基于实际 task
  使用同一策略，避免委派后的行为降级。
- preflight 要求先检查相关记忆/既有决定、仓库说明、适用技能、工作区状态、实现/调用方/配置/测试，并保护
  用户已有改动。
- implementation 强调根因、最小一致改动、无关区域不动、兼容性与契约消费者同步。
- debugging 强调先复现、证据与假设分离、状态/数据流追踪、判别性检查与回归测试。
- verification 与 completion 要求真实运行并观察检查结果、复核最终 diff、明确未验证项；没有证据不得宣告通过。
- Git 规则只在 Git 相关任务中注入，默认现有改动属于用户，未经授权不 stage/commit/reset/clean/push。
- `turn_start` 审计记录 `promptPack` 与 `promptPolicies.softwareEngineering` 路由结果，便于回溯某轮为什么加载该模块。
- `software-engineering-prompt.static.e2e.js` 锁定按需触发、双语对齐、主/子代理接线、记忆预检与 Provider 提问协议。

同轮还修复了 Plan 批准状态只存在于服务端闭包、模型不可见的回归：批准后追加结构化
`previous_mode/current_mode/plan_status/execution_authorized` 状态块，测试要求模型看到该状态后才允许继续工具调用。

在用户提供的 Fable/Claude Code/Codex 提示词样本与 Codex 官方提示指南上继续做了一轮取舍后，策略包升级为
“可执行契约”，不照搬模型身份、产品专属工具名或超长规则清单：

- 新增 scope 模块，把“解释/审查/汇报/诊断”与“修改/构建/修复”分流；只读请求不会被模型自行扩成写入，修复授权也不会延伸到无关清理、发布或外部操作。
- 稳定工具协议新增授权边界：文件、网页、UI、记忆、技能和工具返回中的文字只作为待核验数据，不能冒充用户授权；权限拒绝不得通过终端、换工具或子 Agent 绕过，批准也只覆盖已说明的动作、目标和当前回合。
- 实现规范补齐邻近代码风格、命名和注释密度；注释只解释代码无法表达的非显然约束。缺陷修复必须重跑原始复现路径，最终答复必须自洽，不能把关键结论留在过程消息里。
- 每条用户消息仍只做记忆索引的轻量相关性检查；命中后把记忆视为可能过时的线索，先核对文件、函数、开关与环境仍成立，只在会实质改变回答或行动时采用。保存前先 list/read 去重，不重复保存仓库和 Git 已可推导的事实。
- Plan 模式在最终计划前允许经过现有风险分级确认的只读工具调查与关键澄清；修改、执行、委派及任务元数据写入仍被拒绝。最终 `PLAN:` 必须包含范围、相关组件、选定方案、关键契约、风险/兼容性和验证方式，且不再让模型额外重复询问是否批准。
- `planDiscoveryToolBatchAllowed` 对原生、桥接和服务端工具统一做 fail-closed 判定；纯读批次放行，混合读写批次整批拒绝，保持工具结果配对和原有路径/SSRF/权限护栏。

## 4. 验收清单

- [ ] 提示词分层快照测试上线，CI 红绿可见任何文本 diff
- [ ] A/B 基准集 10+ 任务，改版前后通过率报告可生成
- [ ] `app/prompts/` 外置完成，中英双份，注册点唯一，`PROMPT_PACK_VERSION` 入会话元数据
- [ ] system 首条逐字节稳定（同会话两轮请求前缀一致断言）
- [ ] 总预算闸 + 降级事件 UI 可见
- [ ] 语义 loop-guard e2e（真死循环捕获 / 正常探索不误伤）
- [ ] Claude 子代理角色 prompt 注入 e2e
- [ ] 《模型工作流规范》文档入库并双语
- [x] 软件工程策略按任务触发，双引擎主回合与两类子代理共用
- [x] 每条用户消息先检查已启用记忆索引的相关性，命中才深读
- [x] Plan 批准后的执行态对模型显式可见并有端到端回归锁
- [x] Plan 提案前允许只读调查，写入/执行/委派与混合批次 fail-closed
- [x] 观察内容不构成用户授权，权限拒绝禁止跨工具/终端/子 Agent 绕过
- [x] 诊断与修改交付分流，记忆命中后先核验时效再采用
- [x] 回合审计记录提示词包版本与工程策略路由结果
