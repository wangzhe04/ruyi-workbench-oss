# Durable state inventory

> Generated by `node dev-harness/durable-state-inventory.js --write`; do not edit by hand.

Coverage: **42 surfaces** — shared lifecycle 15, dedicated protocol 12, external ownership 8, regenerable 7.

| ID | Path/pattern | Owner | Schema | Write | Corruption | Capacity | Cache | Recovery | Decision |
|---|---|---|---|---|---|---|---|---|---|
| config | `<data>/config.json` | `01-config.js` | object / configSchema 7 | atomicWriteJson + serialized chain | normalize; parse failure falls back (legacy behavior) | bounded fields in normalizeConfig | uncached by contract | defaults + migration rewrite | **shared-lifecycle** — shared atomic write; read lifecycle retained until direct-test writes gain mtime invalidation |
| runtime-handshake | `<data>/runtime.json` | `13-http-router.js` | ephemeral object | atomicWriteJson | next boot replaces | one record | RUNTIME memory is authority | restart server | **shared-lifecycle** — boot handshake is intentionally ephemeral |
| last-start-error | `<data>/last-start-error.json` | `13-http-router.js` | object / implicit v1 | atomicWriteJson | unknown kind or unparsable is dropped (no notice shown) | one record | START_NOTICE holds the consumed copy for this process | the next failed start rewrites it | **regenerable-exemption** — 118c one-shot boot notice: written only when a start fails, read once on the next successful start and deleted immediately |
| usage-ledger | `<data>/usage/YYYY-MM.jsonl` | `00-boot.js` | NDJSON rows | appendFile through usage chain | bad rows skipped by readers | monthly partition + storage policy | none | retain valid rows; append continues | **dedicated-exemption** — append-only ledger cannot use whole-file JSON replacement |
| audit-log | `<data>/logs/workbench-YYYY-MM-DD.ndjson` | `04-permission-runtime.js` | NDJSON events | single append stream | bad rows ignored by audit readers | logsKeepDays | open stream | rotate by day | **dedicated-exemption** — append stream and rotation are dedicated protocols |
| claude-settings | `~/.claude/settings.json` | `01-config.js` | external object | atomicWriteJson after merge | invalid external file treated as empty | owned keys only | none | Claude keeps primary semantics | **external-exemption** — externally owned merge target; sidecar proves field ownership |
| claude-settings-sidecar | `<data>/claude-settings-sync.json` | `01-config.js` | object / implicit v1 | atomicWriteJson | missing means conservative no-delete | one record | none | recreated on next sync | **shared-lifecycle** — small shared-atomic ownership sidecar |
| kimi-mcp | `~/.kimi-code/mcp.json` | `01-config.js` | external object | atomicWriteJson after merge | invalid external file treated as empty | managed IDs only | none | Kimi retains built-ins | **external-exemption** — externally owned merge target; sidecar proves server ownership |
| kimi-mcp-sidecar | `<data>/kimi-mcp-sync.json` | `01-config.js` | object / implicit v1 | atomicWriteJson | missing means no managed IDs | one record | none | recreated on next sync | **shared-lifecycle** — small shared-atomic ownership sidecar |
| generated-mcp | `<data>/generated/workbench.mcp[.<session>].json` | `01-config.js` | generated object | atomicWriteJson | regenerate | global + per active session | none | generateMcpConfig | **regenerable-exemption** — derived launcher input |
| session-head | `<data>/sessions/<id>.json` | `02-session-store.js` | object / storageVersion 2 | atomicWriteJson + per-session chain | rename to .corrupt on invalid | one per session | session cache/index | load v1 backup or reconstruct v2 head | **dedicated-exemption** — session v2 head has dedicated migration and body coordination |
| session-messages | `<data>/sessions/<id>.messages.ndjson` | `02-session-store.js` | NDJSON messages | append whole newline records | ignore torn tail; reject corrupt interior | message count in head | loaded per request | v1 backup / head counts | **dedicated-exemption** — append-only session v2 body protocol |
| session-provider | `<data>/sessions/<id>.provider.ndjson` | `02-session-store.js` | NDJSON provider history | append whole newline records | ignore torn tail; reject corrupt interior | count in head | loaded per request | v1 backup / head counts | **dedicated-exemption** — append-only session v2 body protocol |
| session-interventions | `<data>/sessions/<id>.interventions.ndjson` | `02-session-store.js` | NDJSON command journal | serialized append + compaction | degraded flag and corrupt-row count | compaction threshold | none | replay valid commands | **dedicated-exemption** — authoritative append/CAS journal |
| session-changes | `<data>/sessions/<id>.changes.ndjson` | `02-session-store.js` | NDJSON change records | serialized append | bad rows isolated from valid rows | session lifecycle | none | replay valid rows | **dedicated-exemption** — append-only change audit |
| session-index | `<data>/sessions/index.json` | `02-session-store.js` | array / implicit v1 | atomicWriteJson; unique sync tmp on exit | rebuild from session heads | one row per session | Map + dirty/debounce | full disk scan | **dedicated-exemption** — exit-time synchronous flush is an explicit exemption |
| session-search-index | `<data>/sessions/_search-index-v1.json` | `13d-core-domain-routes.js` | object / version 1 | atomicWriteJson | missing or wrong version rebuilds from scratch | one 4KB unit per session | entries kept in memory for the current request only | rebuild by re-extracting units from session bodies | **regenerable-exemption** — fully derived search cache: every byte is recomputable from session NDJSON bodies |
| session-v1-backup | `<data>/sessions/<id>.json.v1bak` | `02-session-store.js` | legacy session object | COPYFILE_EXCL | never overwrite | one per migrating session | none | restore legacy session | **dedicated-exemption** — migration rollback artifact |
| journal-index | `<data>/sessions/<id>.journal/index.json` | `02-session-store.js` | array / implicit v1 | atomicWriteJson | rebuild from journal files | journal size cap | journal GC cache | directory scan | **dedicated-exemption** — coordinated checkpoint journal protocol |
| journal-history | `<data>/sessions/<id>.journal/history-*.json.gz` | `10-context-governance.js` | gzip JSON | binary write | drop unreadable snapshot | journal cap | none | other checkpoints/session history | **dedicated-exemption** — compressed binary is not a JSON-file lifecycle |
| kimi-session-index | `<kimi-home>/session_index.jsonl` | `05b-kimi-bridge.js` | external JSONL | Kimi-owned append | adapter skips invalid rows | Kimi-owned | short-lived lookup | Kimi rebuild | **external-exemption** — read-only external engine state |
| kimi-wire | `<kimi-session>/agents/*/wire.jsonl` | `05b-kimi-bridge.js` | external JSONL | Kimi-owned append | adapter tolerates incomplete stream | Kimi-owned | tail cursor | Kimi session replay | **external-exemption** — read-only external engine state |
| storage-trend | `<data>/storage-trend.json` | `06-provider-engine.js` | array / implicit v1 | atomicWriteJson | invalid means empty | 240 hourly samples | none | new samples rebuild trend | **regenerable-exemption** — legacy array remains byte-compatible; derived observability data |
| engine-transcript-index | `<data>/engine-transcripts.json` | `06-provider-engine.js` | object / version 1 | atomicWriteJson + chain | sanitize to empty known set | one row per transcript | process chain only | scan transcript roots | **shared-lifecycle** — shared atomic index with reconstructible authority |
| engine-transcripts | `<engine-root>/<type>/<session>.jsonl` | `06-provider-engine.js` | external JSONL transcript | engine-owned | reader skips invalid lines | engineTranscriptDays | none | engine replay | **external-exemption** — external engine append protocol |
| playbooks | `<data>/playbooks/<id>.json` | `06-provider-engine.js` | object / normalized | atomicWriteJson | invalid playbook skipped | bounded by user files | none | user edits or delete | **shared-lifecycle** — shared atomic write; domain validator owns schema |
| memory-body | `<data>/memory/{global,project/<key>}/<id>.md` | `06d-memory-domain.js` | markdown + frontmatter | atomicWriteJson (markdown string passthrough) | unparsable head yields defaults; oversized (>260KB) file is skipped by the registry | one file per memory; 260KB write-side cap | in-process head cache keyed by size+mtime (113a) | user rewrite or re-propose; sidecars are rebuilt from bodies | **shared-lifecycle** — memory bodies are user-authored content, not derived state: the workbench never regenerates them |
| memory-usage | `<data>/memory/_usage-v1.json` | `06d-memory-domain.js` | object / schema 1 | atomicWriteJson | sanitize to empty entries | entry TTL/cap policy | process state | recompute from use | **shared-lifecycle** — shared atomic write; domain sanitizer owns entries |
| memory-meta | `<data>/memory/**/meta.json` | `06d-memory-domain.js` | object / implicit v1 | atomicWriteJson | invalid memory omitted | one per memory | registry scan | recreate from memory content | **shared-lifecycle** — shared atomic metadata |
| memory-import-marker | `<data>/memory/.acc-memory-import-v1.json` | `06d-memory-domain.js` | object / schema 1 | atomicWriteJson | missing reruns idempotent import | one record | none | repeat idempotent import | **shared-lifecycle** — shared atomic marker |
| memory-proposals | `<data>/memory/proposals/<session>.json` | `06d-memory-domain.js` | object / schema 1 | atomicWriteJson | invalid proposal ignored | one per session | none | regenerate proposal | **shared-lifecycle** — shared atomic transient proposal |
| memory-relations | `<data>/memory/**/_relations.json` | `06d-memory-domain.js` | array / domain normalized | atomicWriteJson | invalid rows filtered | domain bounded | none | re-propose relations | **shared-lifecycle** — shared atomic domain state |
| agent-run-snapshot | `<data>/agent-runs/<session>/<run>.json` | `07-autonomy.js` | object / domain versioned | atomicWriteJson + per-run chain | degraded persistence surfaced | one per run + retention | live run map | events/checkpoint replay | **shared-lifecycle** — shared atomic snapshot with dedicated event recovery |
| agent-run-events | `<data>/agent-runs/<session>/<run>.events.ndjson` | `08-agent-runs.js` | NDJSON events | serialized append | valid prefix replay | compress after terminal age | per-run sequence | snapshot + replay | **dedicated-exemption** — append-only recovery authority |
| project-agents | `<workspace>/.ruyi/agents.json` | `07-autonomy.js` | object / external-compatible | atomicWriteJson | invalid roles skipped | 32 normalized roles | none | user/VCS restore | **external-exemption** — workspace-owned interoperable declaration |
| project-workflows | `<workspace>/.ruyi/workflows.json` | `08-agent-runs.js` | object / schemaVersion 1 | atomicWriteJson | invalid workflows skipped | domain list | none | user/VCS restore | **external-exemption** — workspace-owned interoperable declaration |
| personal-workflows | `<data>/agent-workflows/<id>.json` | `08-agent-runs.js` | object / normalized | atomicWriteJson | invalid workflow skipped | one per workflow | none | user recreates | **shared-lifecycle** — shared atomic domain state |
| context-calibration | `<data>/context-calibration.json` | `10-context-governance.js` | object / schema 1 | DurableJsonStore -> atomicWriteJson | copy byte-for-byte to .corrupt; diagnostic event | 200 factors + 200 window caps | process cache + explicit invalidate | sanitized defaults then safe write | **shared-lifecycle** — 103c representative full-lifecycle migration |
| web-cache | `<data>/webcache/<sha256>.json` | `11-native-tools.js` | object / domain fields | atomicWriteJson | invalid entry is cache miss | storagePolicy.webcacheMaxEntries | none | refetch or offline miss | **regenerable-exemption** — derived cache; shared atomic write |
| proxy-models-cache | `<data>/proxy-models-cache.json` | `13-http-router.js` | object / implicit v1 | atomicWriteJson | invalid is cache miss | provider-keyed | 60s process cache | refetch providers | **regenerable-exemption** — derived cache; shared atomic write |
| overlay-state | `<install>/.overlay-applied.json + .overlay-audit.jsonl` | `13c-overlay-routes.js` | PowerShell applicator protocol | external script atomic/append | applicator rollback/audit | release history | none | overlay rollback | **external-exemption** — owned by separately tested overlay applicator |
| pretender-index | `<data>/sessions/.pretender/projection-index.json` | `13e-pretender-index.js` | object / schemaVersion 2 | atomicWriteJson | invalid triggers rebuild | one materialized projection | stamp-aware process cache | rebuild from sessions/runs/usage | **regenerable-exemption** — fully regenerable materialized index |

## Scope notes

- NDJSON append logs, session v2, compressed binary snapshots and exit-time synchronous writes retain their dedicated protocols.
- External files are merged or consumed without claiming ownership; workbench sidecars track only fields it owns.
- Regenerable caches may treat invalid data as a miss when authoritative inputs can rebuild them.
- `context-calibration` is the 103c full-lifecycle pilot and exercises schema, sanitize, quarantine, capacity, serialized atomic write, cache invalidation and recovery.
