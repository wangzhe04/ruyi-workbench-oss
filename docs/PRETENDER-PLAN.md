# Pretender 3.0 详细规划 v4(2026-07-30,拍板 + 三轮评审修订)

状态:**已拍板 v4(2026-07-30)**。四轮决策:
- **第一轮(拍板)**:D1=方案 B(mission.id 稳定主键)、D2=做叙事镜头 v1 规则版、D3=先发 Escapade 2.3.0 再进 P1(§6)。
- **第二轮(外部评审修订)**:一份第三方规划评审的事实性指控经主会话逐条亲验代码**全部属实**(决策语义四类不统一:question 409 / permission 404 / plan HTTP 200+ok:false / pool 另一套 409;`/api/missions` 与 `/api/interventions` 每请求全量 readdir;波次算术 14→15)。处置:**目标 1 重述**(1:N 移 3.1,F1 拍板)、**降级序冻结**(F2 拍板)、新增统一决策契约与 75b 规模门、安全/i18n/a11y 改为每波 DoD、门语义三分(§6)。
- **第三轮(多代理对抗审判修订)**:四路只读代理(架构/工程/范围攻击 + steelman 辩护)+ 独立裁决,17 条发现裁决 **13 confirmed / 3 plausible / 1 refuted**,总裁决 **revise-major**。主会话对全部 confirmed 证据逐条亲验属实后修订:**契约幂等持久地基**(T1 拍板)、**决策路由改形带 missionId 段**(T2 拍板)、Intervention seq/settle 字段/撕裂尾行焊行/双壳跨路径四处数据缺口入 74–75 波交付清单、75b 阈值门前置成文、北极星指标可测化并删除「成文豁免」后门、C2 死线加日历兜底、stall 类型补拍板程序、工期加 DoD 乘数与时钟锚点(§6)。
- **第四轮(契约与门语义复核)**:补齐 Intervention 状态 CAS 与崩溃中间态、规定旧端点必须收敛到同一 command core、以 Mission 级 `changeSeq` 取代脆弱的复合 lastSeenCursor、把三层门细化为五层、拆分第75波、收紧 1:N「零迁移」表述,并把双壳日历死线改为强制决策日而非无条件删除日(§6)。

依据:`docs/UI-VNEXT-CONCEPT.md` §8–§11、`docs/PRETENDER-GATE-REVIEW.md`(第56波评审,GO 有条件 4 条)、路线图 EC-E 三切片(70/71/71b/72)已交付事实。
配套:`docs/PRETENDER-GATE-REVIEW.md`、`docs/OPTIMIZATION-ROADMAP.md`。
本文书是「规划」不是「承诺」:波次划分可随实施反馈重切,但**范围冻结项、四条附加条件、非目标、双壳退出死线、§6 四轮拍板项、§8 降级序**不得静默修改,改动必须成文。

---

## 1. 总策略

**一句话:换容器,不换引擎;投影先行,壳层渐进置换;契约先于 UI;每一步都有退路。**

- 执行资产(双引擎/工具/权限/检查点/审计/技能/记忆/离线存储)**零重写**——概念稿 §8.3 非目标第 2 条冻结。
- 新壳层全部状态来自 EC-E 已交付的持久化投影,**禁止前端拼装散装端点、禁止模型文案猜状态**(go 条件 #1/#5,no-go 触发器 #1/#3)。
- **契约先于 UI**:跨壳共享的不是「现有 REST API」这个现实,而是**统一领域契约与唯一 command core**——P1 冻结 `POST /api/missions/:missionId/interventions/:id/decision`,以 `(missionId, interventionId, expectedVersion)` 对权威状态做 CAS;`idempotencyKey` 只标识同一请求重试。经典端点与新端点都必须调用同一个 `decideIntervention()` 核心,不得各自执行后再补账;P3 UI 只接新契约。
- 双壳并存期:新功能只实现一个业务入口,经共享 command 层被两壳调用,**禁止两套业务状态**(§9.2)。
- 每波沿用本仓纪律:红绿双证 e2e、冻树全量门、主会话亲跑验收、src/ 改动主树串行(server.js 是 build.js 拼接产物,手术面是 src/ 模块 + 产物字节级重建验证,不派并行 worktree——v3 更正 v2 的「11k+ 行单文件」过时表述)。

## 2. 范围冻结(拍板项)

### 2.1 3.0 范围 = 概念稿 §8.2 六目标(v2:目标 1 重述),逐条落位

| # | 目标 | 落位阶段 | 现状基础 |
|---|---|---|---|
| 1 | **稳定 Mission 身份**(F1 拍板):missionId 为一级主键,一单关联多 Run/多引擎;**「一单多会话(1:N)」正式移 3.1**——3.0 保证存量 Mission 身份无需重映射,不承诺 3.1 无结构升级 | P1 主键落地 + P2 壳层 | 投影已存在;1:N 仍需 Mission→sessions 解析、共享预算/验收/回滚边界与存储升级,不在 3.0 假装交付 |
| 2 | 一个全局「需要你」收件箱 | P0✅只读 → P1 统一契约(带权威状态CAS) → P3 决策动作 | `/api/interventions` 已聚合;决策语义四类不统一,P1 契约收口 |
| 3 | 始终诚实的反悔能力 | P3 反悔柄 | revertible 标记 + irreversible 正向账 + checkpoints 引用已就位 |
| 4 | 镜头制取代功能隐藏 | P2 原始镜头 → P3 班组图/叙事 | 消息流渲染器零改动平移;五态派生纯函数已钉死 |
| 5 | 收工闭环(收工卡+沉淀) | P3 | mission.result 结果章已定格;熟活/记忆数据层不动只换陈列 |
| 6 | 复用 Escapade 执行资产 | 全程红线 | ≈70% 渲染资产平移估计维持 |

### 2.2 非目标(照概念稿 §8.3 原文冻结)

不引云账号/协作/移动端/遥测/新工具宇宙;不为大版本重写执行资产;不绑定前端框架(零运行时依赖,改动须另立 ADR);不用模型摘要冒充权威状态;**P2 Preview 公开前不把 Pretender 名/3.0 版本号混入用户界面与发布物**。
v2 补充:**3.0 不交付 1:N 多会话 Mission**(移 3.1,见目标 1);conflict/stall/budget 三类 Intervention 在 3.0 只有 schema 保留位,**无生产者,不得在任何 UI/文档中暗示其可用**;概念稿 §8.2-2 的「冲突处理」分句随 conflict 保留位一并按目标 1 同款程序重述——**3.0 交付四类(permission/question/plan/pool),conflict 标注 3.1**,发布物禁用语同步。

### 2.3 评审四条附加条件 → 冻结为硬门

| 条件 | 冻结形态 | 卡在哪一关 |
|---|---|---|
| C1 性能门前置 | EC-D 性能硬门(真实 Edge 首屏 <1.5s、视图切换 P95 <200ms、事件流增量渲染)对新壳层复跑全绿;**75c 规模门(大数据集)先行**,性能门不再只测小数据集 | **P2 Preview 公开前的放行闸**,不过不公开 |
| C2 双壳退出死线 | 新壳层默认后**最迟第 2 个公开 Release 且不晚于 3.0.0 发布后 6 个月进入强制退出评审**。三件套与兼容门全绿则退出;若仍有 P0/P1 红项,不得按日历强删,而是恢复经典默认或阻断下一公开 Release并成文整改期。退出前交付数据导出+回滚说明+兼容报告,工作量登记入 post-3.0 线估时 | post-3.0 退出线(原第88波,不计入 3.0 交付工期) |
| C3 全局决策动作范围 | 跨会话决策必须经过 P1 统一契约。**权威唯一键是 `(missionId, interventionId)`**,以 `expectedVersion` 做 `pending→applying→terminal` CAS;`idempotencyKey` 仅用于同请求重试。相同 key 重放返回原结果,不同 key 命中已终态返回 409,过期 410,不存在/归属不符 404,不可送达 409-with-reason。崩溃遗留 `applying` 进入 `indeterminate/cancelled_restart`,绝不自动重放高风险动作。经典旧端点必须适配到同一 command core;混合路径并发决策不重复执行 | 75a–75b 契约 + 81 波验收断言 |
| C4 残余竞态挂号 | provider 回合进行中路由突变 mission 被回合收尾盖回(第72波记录在案) | **P1 内修复**,不得带入 3.0 |

### 2.4 每波 Definition of Done(v2 新增——安全/i18n/a11y 不再集中到 P2/P4 末尾)

每一波(自第74波起)提交前必须同时满足:
1. 新交互键盘可达、焦点可见、读屏有名词(ARIA),双语键齐全(locale 扁平键纪律,无 [key] 渲染);
2. 新写路径带威胁标注:权限边界、token 鉴权、跨会话归属校验;**批准类动作必须带上下文、作用域、过期状态与二次确认策略**;
3. 红绿双证 e2e + 冻树全量门绿 + 主会话亲跑(既有纪律,成文固化);
4. 经典壳层零回归(P2 起);
5. 新字段/新端点在 schema 文书内有对应条目(P1 出门后)。

第80/86波的「走查/硬化」因此是**终审**而非第一次补齐——终审仍抓出系统性缺失 = 该波 DoD 执行失实,记账。

## 3. 五层门语义与北极星指标

评审指出:第56波 31 条 e2e 证明的是**数据正确**,不能证明可理解性与旅程改善。门重新分层:

| 门 | 含义 | 落位 |
|---|---|---|
| **Projection Ready** | 权威数据投影、五态与四旅程数据面可重复验证 | P0 ✅(EC-E + 56 波) |
| **Data & Contract Ready** | schema、权威状态 CAS、统一 command core、重启/崩溃语义、规模门有 e2e 钉死 | P1 出门闸 |
| **Preview Ready** | 新壳真实渲染;尚未原生交付的决策/回滚能准确回经典壳层完成,双壳状态一致;性能与回退实证通过 | P2 出门闸 |
| **Product Ready** | 决策、反悔、控制面和收工闭环均在新壳真实操作;人因验证通过 | P3 出门闸 |
| **Release Ready** | 安全终审、迁移/恢复演练、离线包与长期数据压力全绿 | P4 |

**北极星可观测指标**(P2 起本机测量,不引遥测;系统指标与人因指标分账,代理证据不得冒充真实用户结论):
- **系统正确性**:回来摘要对播种 change records 的覆盖率 100%,无重复、无游标倒退;e2e 可证;
- **系统可达性**:从任意视图到待决事项 ≤2 击,交互与渲染耗时 ≤5s;scripted 可测;
- **人因理解**:用户从打开任务到正确复述「离开后发生了什么」目标 ≤10s;对可回滚/部分恢复/不可逆判断正确率 ≥90%。第80波前冻结受试者来源、任务脚本与问卷,第85波执行;无外部受试者时双人盲测只能记为 Preview 代理证据,**不能标记 Product Ready**;
- **恢复可靠性**:刷新/重启后续办不丢权威状态,目标 100%;e2e 可证。

## 4. 波次规划 v4(P0✅ → P4,自第74波起,3.0 交付线 16 个切片)

波次号是内部序列,与版本号解耦。P2 起每波退出条件含「经典壳层零回归」与 §2.4 DoD。

### P1 · 领域基础收尾 + 契约与规模门(第74–75c波)

**第74波 · schema 冻结文书 + 威胁模型 + 权威状态协议**
- 交付:`docs/PRETENDER-SCHEMA.md`——覆盖 `docs/UI-VNEXT-CONCEPT.md` §9 的 Mission projection / Intervention 全字段,冻结字段类型、权威来源、可变性、事件语义、schema version 与迁移契约。missionId 按 D1 成文,明确 3.1 的1:N只保证身份不重映射,不承诺存储零升级。
- **Intervention 状态协议**:
  - 权威键=`(missionId, interventionId)`,版本=`interventionVersion`;状态转换=`pending → applying → terminal`,并定义崩溃恢复态 `indeterminate/cancelled_restart`;
  - `expectedVersion` 参与 CAS;`idempotencyKey` 是请求重试键而非业务唯一键。相同 key 返回已存响应,不同 key 命中终态返回 409;
  - 决策 payload 采用按 type/action 区分的联合结构(permission allow/deny+updatedInput、question answer、plan/pool approve/reject),禁止一个松散 action 对象吞掉类型差异;
  - 状态转换、审计和响应的落盘顺序与失败注入矩阵成文。重启遇到 `applying` 不自动重放批准、工具或池物化,而是对照执行审计后进入诚实终态。
- **Mission 变化协议**:建立持久、单调的 `mission.changeSeq`/`projectionRevision` 及轻量 change record;turn/run/Intervention/预算/result/rewind/删除等影响任务事实的变化均推进同一序列。各源 cursor 只作溯源证据,不再拼成用户 lastSeen 的权威游标。
- **类型与控制面**:冻结 `permission|question|plan|pool` 的3.0生产语义;`conflict|stall|budget` 仅保留名字且消费者必须容忍未知类型,不预冻结未来 payload。Mission 暂停/继续/停止/重试/人工接管逐动作界定作用域。
- **安全与规模前置**:跨会话伪造/重放/越权/过期竞态/不可送达威胁及对策成文;75c 压力门的硬件、冷热口径、数据档位、P95阈值和矩阵剪枝规则在本波冻结,后续实测只许收紧,放宽须回本文书记录理由。
- 退出条件:文书字段与现有 e2e 可逐条对照;每个威胁、崩溃窗口与数据缺口均落到75a/75b/81的实现或验收项。
- 风险:中。虽以文书为主,但包含不可逆的状态协议和测试阈值决策,不得按低风险走查。

**第75a波 · Mission/Intervention 权威存储原语 + missionId + C4**
- 交付:
  - **missionId 派生**(方案 B):session 主键与文件布局不动;新会话 `missionId=sessionId` 派生写入;`normalizeSession` 存量只读派生不回写;`/api/missions`、`/api/interventions` 对外标识从第一天以 missionId 命名;
  - **C4 竞态修复**:provider 回合内 mission 写与回合收尾盖回的定序,红绿双证;
  - **权威 Intervention journal**:同 Mission 写链串行;记录完整状态而非后写整行丢字段;物理截断撕裂尾行后再 append;每次转换推进 `interventionVersion` 与 `mission.changeSeq`;
  - **原子 CAS 原语**:实现 pending/applying/terminal/indeterminate 转换和服务器侧 canonical key;失败注入覆盖「写 applying 前/后、resolve 前/后、terminal 前/后」。
- 退出条件:missionId 三处一致;存量只读派生不回写;C4 绿;坏尾自愈、字段保留、seq 单调、并发 CAS、崩溃恢复 e2e 全绿。
- 风险:中高。触碰存储与 provider 收尾路径,主树串行。

**第75b波 · 统一决策 command core + 四类适配**
- 交付:`POST /api/missions/:missionId/interventions/:id/decision`;四类 type/action payload、expectedVersion、idempotencyKey、统一错误信封和审计字段。**经典 question/permission/plan/pool 端点只保留为兼容适配器,必须调用同一个 `decideIntervention()`**,不得直写内存 Map 或自行 settle。
- pool paused-pending:恢复前返回不可送达409及稳定 reason,恢复后经同一 core 决策;不把“暂不可送达”误报为不存在或已过期。
- 退出条件:四类经统一入口全通;同 key 重试返回原结果;不同 key/混合路径并发只发生一次状态转换与一次实际动作;旧端点回归零红;归属伪造、版本冲突、过期、不可送达和 applying 崩溃恢复均有 e2e。
- 风险:中高。适配四套既有执行路径,71/71b 幂等和安全默认不得回退。

**第75c波 · 可重建索引、分页与规模门**
- 交付:Mission/Intervention **可重建物化索引**(权威源仍是 journal);列表 cursor 分页 + 基于 `projectionRevision` 的 ETag/快照版本;NDJSON 压缩;数百 Mission、数万 Intervention 行、十万级 usage/event 行的列表/详情/全局收件箱冷热与低配测试。分页 cursor 必须携带索引 revision,跨页期间 revision 变化时明确重开快照,不得静默漏项/重复。
- 索引重建的一致性比较只覆盖持久事实;`activeTurn` 等易失 live overlay 单独叠加并标 freshness,不纳入“索引等于权威账”的静态断言。
- 退出条件:按74波阈值全绿;删索引自动重建与权威 journal 一致;坏索引/中断重建可恢复;分页快照、ETag、live overlay 和压缩前后等价 e2e 全绿。
- 风险:中。索引只能是可丢缓存;若 journal 有损必须显式报 degraded,不得用重建成功掩盖。

**P1 出门闸(Data & Contract Ready)**:schema + missionId + `mission.changeSeq` + 权威状态 CAS + C4 + 统一 command core 四类全通(含混合路径/崩溃窗口)+ 规模门全绿。此后任何 UI 不得引入文书外字段。

### P2 · 新壳层 Preview(第76–80波)

原则:原始镜头先行;Preview 期新壳层默认关、经设置开启,经典壳层默认。

**第76波 · 壳层骨架**:案头条(四事实)+ 任务坞(72px 瓷章轨三态)+ 主台单视图容器 + 新/经典切换(持久化,默认经典)。五态派生复用 `mission-state.js`。退出:骨架 e2e(切换持久化、瓷章三态真实投影驱动);经典 DOM/测试零改动。风险:低-中。
**第77波 · 任务单(原始镜头)**:全宽任务单=头部(五态+工序进度+用量)+现场(消息流渲染器原样嵌入)+收活台初形(需要你/成果/台账只读)。退出:四旅程数据面完整;渲染器复用断言;长任务 cursor 续断点;SSE 增量在新容器焦点/滚动稳定。风险:中。
**第78波 · 交办台首页 + 交办确认卡**:交办箱→确认卡(复述/工作圈/本单安全档/有依据才显的估计)→开工立单→瓷章落坞;**速问逃生舱**;熟活架;续办条;最近收活;首跑引导态原位。退出:确认卡→立单全链路 e2e;逃生舱 e2e(quick_ask 不进 missions)。风险:中。
**第79波 · 「你离开后发生了什么」+ 档案视图**
- 交付:**lastSeenRevision**——用户视图状态单独存入本机 UI-state store,按 missionId 记录,不写回 Mission 事实或旧 Session。任务视图/回来摘要成功呈现后才推进至当前 `mission.changeSeq`;拉取失败、页面未完成渲染或 changeSeq 间断时不得误标已读。
- 回来摘要按 `(lastSeenRevision,currentRevision]` 的 change records 确定性呈现新增进展/失败/预算变化/待决变化/成果/rewind/run删除,每条保留原始源 cursor 供展开,零模型摘要。**档案视图**:已收工任务网格 + 搜索/筛选/置顶/归档/按状态与工作圈分组。
- 退出条件:播种各类 change record→摘要覆盖100%、无重复、无倒退;changeSeq 缺口触发 degraded/重建而非静默漏项;UI-state 损坏只丢“已读位置”不损 Mission;档案操作 e2e;北极星系统指标首测。
- 风险:中。依赖75a的统一变化序列;视图状态与领域事实必须严格分层。
**第80波 · 双壳回退 + 性能门复跑(C1)+ Preview 走查(终审)**
- 交付:壳层切换迁移说明与故障回退(切壳≠数据迁移,各自独立回退);**C1 性能硬门对新壳层复跑**(含 75c 大数据集门下的首屏与切换);Preview 走查——按 §2.4 DoD 终审 + USER-GUIDE 新壳层章节 + 冻结项核查(UI/发布物无 Pretender 名/3.0 版本号)。
- 退出条件:**C1 全绿 = Preview 公开放行闸**;回退路径 e2e;文档就位。
- 风险:中。性能门首轮抓红是常态,预留修复余量。

**P2 出门闸(Preview Ready = Preview 可公开)**:C1 绿(含规模门)+ 四旅程新壳层可走完;尚未原生交付的决策/回滚由新壳准确跳转经典壳层完成真实操作,两壳终态即时一致;旧数据双壳完整交互、回退实证、文档和 Preview 人因代理证据就位。不得宣称新壳已完成全局决策或反悔闭环。

### P3 · 完整任务交付体验(第81–85波)

**第81波 · 全局「需要你」决策动作(C3 收口)**:鎏金抽屉内直接决策,**只接 75b 统一契约**;跨会话决策后各会话经典 UI 同步;停工卡 v1(人话+再试/换法子/先算了);批准动作二次确认策略按 74 波 threat model 落地。退出:相同 idempotencyKey 重试返回原结果,不同请求命中终态统一409且不重复执行;绝不默认批准断言沿用71/71b。风险:中高。
**第82波 · 班组图镜头**:P3 画布稿语义换装(节点=队员、工头一句话、虚线鎏金拟增工序=池审批 Intervention 的图内表达);递话。退出:多 Agent 旅程图内可监督;图=数据的面,无第二状态。风险:中。
**第83波 · 叙事镜头 v1 + 本地通知(flag)**:规则折算纯函数(动词人话化+对象+时间线,每句可展开原始卡,双导出);**本地「需要你」通知**(浏览器/Windows 通知+免打扰+同 Intervention 去重,feature flag 默认关——**降级序首砍项**)。退出:折算每句可溯源;增量 append 不全量重建;通知若未按降级序砍掉,须补权限拒绝、免打扰边界、同 Intervention 去重、终态撤回与应用重启不补炸历史通知的 e2e。风险:低-中。
**第84波 · 反悔柄 + 台账时间轴 + Mission 控制面**:台账=改动时间轴(每条回退钮,revertible 真实标注,不可逆账显性列出);整单回退常驻;档案保留反悔柄;**控制面动作按 74 波语义**(暂停/继续/停止/重试/人工接管,作用于回合/Run/整单在 UI 上无歧义)。退出:go 条件 #6 e2e(每个钮有真实能力);控制面动作语义 e2e。风险:低-中。
**第85波 · 收工卡闭环 + 专项可用性验证**:收工卡(验收/成果/未完成项/变更/用量/审计,源=mission.result)+存为熟活+记住习惯;长任务离场/回来、失败恢复、人工接管专项验证;**北极星四指标全测**。退出:四旅程完整新壳层闭环;go 条件 #1–#7 复评通过。风险:中。

**P3 出门闸(Product Ready)**:go #1–#7 全绿;C3 关闭(含混合路径);决策、反悔、控制面、收工闭环全部在新壳真实操作;北极星系统指标与真实受试者人因指标按 §3 规程达标。仅有内部双人代理证据时可继续 dogfood,不得进入 P4 Release Ready。

### P4 · 硬化与切换(第86–87波;88 移 post-3.0)

**第86波 · 硬化终审**:安全红队**终审**(XSS 纪律对新视图域复审——日常 DoD 已前置,此波不补窟窿);性能终验;双主题/双语/a11y 终审;离线升级(overlay 对新壳层载荷);数据迁移与恢复演练。
**第87波 · 默认切换 + 3.0.0 发布**:新壳层默认开(经典可切);版本三角 bump 3.0.0;CHANGELOG/USER-GUIDE/发布物正名;发布门同 2.2/2.3 三门。
**post-3.0 退出线(原第88波,不计入 3.0 交付工期)**:新壳默认后第 1 个公开 Release 保留经典;最迟第 2 个公开 Release 且不晚于 3.0.0 后6个月进入强制评审。三件套与兼容门全绿则退出;仍有P0/P1红项则恢复经典默认或阻断下一公开Release并给出整改期,**不得为了日历强删安全退路,也不得维持新壳默认后静默延期**。原始镜头保留为专家入口。

## 5. 版本与发布线关系

- **D3 拍板:先发 Escapade 2.3.0,再进第74波。** P1 启动前置门 = v2.3.0 tag 推送完成。2.3 发布后 P1 若出问题有干净的已发布回退点。
- Preview 期(76–80)对外仍是 Escapade 2.x;3.0.0 只在第87波落地。
- 若中途需发 Escapade 2.3.x/2.4(修 bug/小特性),走既有发布门,与 Pretender 波次互不阻塞。

## 6. 拍板记录(四轮,全部定案)

**第一轮(2026-07-29)**:
- **D1 · mission.id 主键 = 方案 B**:引入 `missionId` 稳定主键,session 主键/文件布局不动,新会话 `missionId=sessionId` 派生(1:1),存量只读派生不回写;schema/API 从第一天以 missionId 为对外名。
- **D2 · 叙事镜头 = 做**:P3-83 v1 规则版(不调模型摘要、不承诺散文)。
- **D3 · 启动顺序 = 先发 2.3.0**:P1 前置门 = v2.3.0 tag 推送完成。

**第二轮(2026-07-29,外部评审修订)**:
- **F1 · 目标 1 重述(拍板)**:「一单多会话 1:N」正式移 3.1;3.0 目标 1 = 稳定 Mission 身份(missionId 主键 + 多 Run/多引擎关联)。理由:1:N 的执行语义(多会话共享预算/验收/回滚边界)是新领域,塞进 P1 会破坏「P1 只收口」原则;missionId 使存量身份无需重映射,但3.1仍需解析索引与存储升级。**不得在 3.0 发布物中宣称 1:N 已交付或“零迁移”。**
- **F2 · 降级序冻结(拍板)**:见 §8。
- **评审采纳项**:统一决策契约(75);75b 规模门;DoD 制度化(§2.4);门语义三分+北极星指标(§3);回来摘要+档案视图(79);Mission 控制面(74 语义/84 动作);Intervention 类型全集冻结含保留位标注(74);本地通知 flag(83);波次算术修正(15 波,88 移 post-3.0);工期区间化(§9)。

**第三轮(2026-07-29,多代理对抗审判修订)**:
阵型:三路只读攻击(架构与数据一致性/工程可行性与工期/范围决策与降级序)+ steelman 辩护 + 独立裁决官(file:line 证据抽查);全部 confirmed 证据经主会话亲验。裁决 **revise-major**——骨架(投影先行/missionId 身份稳定/降级序)经得起对抗,缺口集中在契约地基与门语义。
- **T1 · 契约幂等持久地基(拍板,后由第四轮 S1 收紧)**:统一契约的409/404/410不能依赖fire-and-forget旁路账;原提案以 idempotencyKey→终态 decided-set 为地基,第四轮进一步改为 Intervention 权威键+版本 CAS。
- **T2 · 决策路由改形(拍板)**:契约路由 = `POST /api/missions/:missionId/interventions/:id/decision`(带 missionId 段)。原冻结路由 `POST /api/interventions/:id/decision` 无会话段,与按 sessionId 分片的存储相撞,`:id` 解析只能全目录扫描。
- **T3 · stall 类型补程序(拍板)**:第56波评审明确把「是否新增 intervention type stall」留作 P3 决策;v2 的枚举冻结把 stall 标为保留位,程序上等于未经拍板关闭该决策。拍板:**3.0 确认无 stall 生产者**(理由:停滞检测尚无权威信号源,造出来的 stall 是猜测不是事实,违反 no-go #3),**「是否立项 stall 检测」作为 P3 显式决策点保留**,枚举保留位不变。
- **审判采纳项**(全部 confirmed,已落位各节):Intervention seq + 纳入 cursor(74);settle 记录携带 type+requestedAt(74/75);NDJSON 撕裂尾行自愈前移到契约上线前(74/75);账本权威级别与 boot 终态化信任边界成文(74);pool paused-pending 契约语义成文(74);混合路径并发决策e2e(75);75b阈值前置;北极星指标可测化;C2日历兜底;冲突处理重述;DoD成本乘数;新增账本有损风险。**其中 idempotencyKey 权威、复合 lastSeenCursor 与 P1 切片编号已由第四轮 S1/S3/S5 覆盖,本条只保留审判历史。**
- **审判 refuted 一条留档**:「pool 决策仅 live 可送达与四类全通门结构性冲突」被裁决官驳回(规划已有 409-with-reason 不可送达语义)——但两位裁决官意见相左,主会话调和:响应语义已覆盖,「四类全通」闸的 pool 分支测试构造方式仍按 74 波清单成文,按 confirmed-but-mitigated 处置。
- **plausible 三条留档**(不阻断,降级序底线形态成文/降级序类型保留位落位/§8.2-2 重述——末者已随采纳项修复,前两条接受为已知文书边界)。

**第四轮(2026-07-30,契约与门语义复核)**:
- **S1 · Intervention 权威状态 CAS(拍板,覆盖 T1 细节)**:`(missionId,interventionId)` 是业务唯一键,`interventionVersion` 做 CAS;`idempotencyKey` 只识别同一请求重试。状态机含 `applying` 与崩溃后的诚实终态,不承诺无法证明的跨崩溃 exactly-once。
- **S2 · 单一 command core(拍板)**:经典四端点与新契约必须调用同一 `decideIntervention()`;旧端点是兼容参数适配器,不是第二条执行路径。混合路径测试因此验证同一实现的原子性,而非两套补丁碰运气。
- **S3 · Mission changeSeq(拍板)**:以持久 Mission 级变化序列统一回来摘要、ETag、分页 revision 与索引失效;run/turn/Intervention 原始 cursor 降为溯源证据。`lastSeenRevision` 属于本机 UI-state,不得写回 Mission 事实。
- **S4 · 五层门(拍板)**:P0 Projection Ready→P1 Data & Contract Ready→P2 Preview Ready→P3 Product Ready→P4 Release Ready;P2 只承诺新壳到经典动作的真实 fallback,直接决策与反悔闭环在P3验收。
- **S5 · P1 拆分与1:N边界(拍板)**:原第75波拆为75a权威存储、75b command core、75c规模门;3.0只保证存量 Mission 身份无需重映射,不得宣称3.1结构零升级。
- **S6 · 双壳日历兜底(拍板)**:6个月是强制退出评审日。门绿则退出;有P0/P1红项则恢复经典默认或阻断发布并整改,不得按日历强删安全退路,也不得静默延期。

## 7. 风险登记册

| # | 风险 | 缓解 | 等级 |
|---|---|---|---|
| R1 | 状态派生误判(纯问答硬套五态) | 速问逃生舱是 78 必须品;kind 显式标识已就位 | 中 |
| R2 | src/ 模块手术 + 产物字节级重建(75a/75b/81 波;server.js 是 build.js 拼接产物非单文件源码) | 主树串行,不派 worktree;产物重建验证随每波 | 中 |
| R3 | 双壳维护税与退出条件冲突 | C2 强制评审日;门绿退出,门红恢复经典默认或阻断发布;契约单一入口;每波经典零回归闸 | 中高 |
| R4 | 性能门首轮抓红 | 75c 规模门提前暴露架构问题,阈值74波前置;80预留修复余量 | 中 |
| R5 | 叙事镜头质量天花板 | v1 只承诺动词人话化+时间线;每句可展开原始卡 | 低 |
| R6 | 隐喻过度 | 装饰不承载信息;节点数据全来自既有字段 | 低 |
| R7 | 专家迁移阻力 | 原始镜头一键直达+工房抽屉+Ctrl+K;经典过渡 Release | 中 |
| R8 | 文档重写量 | 80 波 USER-GUIDE 新章节为 Preview 公开闸 | 低 |
| R9 | **统一契约适配破坏既有幂等** | 71/71b为基线;旧端点强制适配到同一 command core;canonical key+版本CAS+混合路径并发测试 | 中高 |
| R10 | **索引成为第二状态机** | 可重建物化;持久事实与live overlay分层;删索引重建一致性e2e;权威源永是journal | 中 |
| R11 | **Intervention journal/执行动作崩溃分裂** | pending→applying→terminal协议;失败注入;applying重启不自动重放;执行审计对账后诚实终态 | 高 |
| R12 | **复合游标倒退或漏掉跨域变化** | Mission级持久changeSeq统一变化时间线;源cursor只作证据;缺口显式degraded | 中高 |

## 8. 降级序(F2 拍板冻结)

工期/质量压力下的砍单顺序,**先砍表达层,永不砍交办台本体**:
1. **本地通知**(83 波 flag 项,首砍);
2. **叙事镜头 v1**(83 波主体,移 3.1);
3. **班组图镜头**(82 波,移 3.1——多 Agent 监督退化为原始镜头+池审批卡);
4. 以下**永不砍**:回来摘要(79)、档案视图(79)、权威状态CAS与统一决策契约(75a/75b)、规模门(75c)、反悔柄与控制面(84)、收工卡(85)——它们是「交办台」本体,砍了就不是 Pretender。

## 9. 总览表与工期

| 阶段 | 波次 | 核心交付 | 出门闸 |
|---|---|---|---|
| P0 ✅ | 56 | 评审 GO + 全局收件箱 + 五态 + PoC | **Projection Ready** |
| P1 | 74,75a,75b,75c | schema+状态协议 / missionId+C4+存储原语 / command core / 规模门 | **Data & Contract Ready** |
| P2 | 76–80 | 骨架/任务单/交办台/回来摘要+档案/回退+性能门 | **Preview Ready** |
| P3 | 81–85 | 全局决策/班组图/叙事+通知/反悔柄+控制面/收工卡 | **Product Ready** |
| P4 | 86–87 | 硬化终审/3.0.0 默认切换 | Release Ready |
| post-3.0 | (原88) | 双壳退出三件套 | C2 死线 |

工作量:**3.0 交付线 16 个切片**(74、75a–75c、76–87)。工期给区间不外推;**时钟锚点=v2.3.0 tag 推送完成之日**,2.3.0发布执行成本另计。纯后端P1按近期节奏1–2切片/工作日;UI密集P2/P3按0.5–1切片/工作日,再乘§2.4 DoD成本系数×1.3;另列dogfood、Preview反馈与缺陷缓冲约30%。**净估4–7周到87波**;若真实受试者招募或75a崩溃协议抓红,按门等待而非压缩。post-3.0退出线另计,强制评审日取第2个公开Release与3.0.0后6个月孰早。75a/75b/81与80性能门是串行瓶颈。
